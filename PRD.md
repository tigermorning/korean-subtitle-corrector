<!-- lang-switcher:start -->
<p align="center">
  <a href="PRD.en.md">English</a>
  ·
  <a href="PRD.zh-CN.md">中文(简体)</a>
  ·
  <a href="PRD.zh-TW.md">中文(繁體)</a>
  ·
  <a href="PRD.ja.md">日本語</a>
  ·
  <a href="PRD.es.md">Español</a>
  ·
  <a href="PRD.ar.md">العربية</a>
</p>
<!-- lang-switcher:end -->

# 한국어 띄어쓰기·맞춤법 자동 교정 CLI 도구 — PRD (초안)

## 문서 구성 (2026-07-24 모듈화)

이 PRD가 너무 커져서(400줄 가까이) 아래처럼 나눴다. 이 파일에는 **잘 안 바뀌는 핵심 스펙**만 남기고, 계속 쌓이는 실사용 로그·한계 목록·검증표는 별도 파일로 옮겼다.

- **`PRD.md`(이 파일)**: 배경/목적, 범위, 입출력, 기술스택, 교정 판단 엔진의 핵심 설계, 워크플로우, 완료 기준, 의존성, 로드맵, TBD.
- **`docs/KNOWN_LIMITATIONS.md`**: §5에서 분리. 실사용 중 발견한 오탐지/버그와 그 수정 원칙, 아직 미구현인 한계를 사례별로 기록한 목록 (구 §5 "알려진 한계 / 유지보수 요구사항").
- **`docs/log-archive/2026-h2.md`**: 구 §13~§27. 실제 텍스트로 감수하며 발견한 버그와 수정 경위를 날짜순으로 기록한 로그. 크기가 다시 커지면 반기별로 아카이브하는 규칙이 파일 안에 명시되어 있음.
- **`docs/GRAMMAR_PRECEDENTS_TABLE.md`**: 구 §18. 아직 코드로 구현되지 않은 문법 규칙에 대해 온라인가나다로 미리 조사해 둔 검증표.
- **`subtitle_corrector/gananda_precedents.py`**: 이미 코드가 다루는 표현의 온라인가나다 판례(코드와 함께 있어야 하는 것).

- **`docs/BACKLOG.md`**: 밀린 작업 목록(2026-07-27 분리). 세션 시작 시 가장 먼저 읽는다.

새 세션은 이 파일 다음에 위 문서들을 순서대로 훑는 것을 권장한다. 자동화(스킬/예약 작업)가 참조하는 정확한 경로이므로, 이 구조를 다시 바꿀 경우 아래 목록도 함께 갱신해야 한다: `.claude/skills/`의 4개 스킬, `~/.claude/scheduled-tasks/gananda-precedent-research/SKILL.md`, `AGENTS.md`.

**내용을 담은 PRD는 이 파일 하나뿐이다. 사본을 만들지 말 것(2026-07-27 결정).**

전에는 `Documents` 폴더에 이 파일 전문을 복사해 두고 세션마다 동기화했다. 다른 도구(OpenCode)로 이어받을 때를 대비한 것이었는데, 그 방식이 실제로 사고를 냈다 — 2026-07-17에 다른 환경에서 한 작업이 그 사본에만 기록되고 저장소 코드에는 4일간 반영되지 않았고(`docs/log-archive/2026-h2.md` §11 주의), 2026-07-27 전문 대조에서는 그 사본에 §12 사투리 교정 섹션이 통째로 빠져 있었다. 이미 구현된 기능인데 사본만 읽은 세션은 없다고 판단할 수 있는 상태였다. **2026-07-27 사용자 확인: 이 프로젝트는 앞으로 Claude Code로만 작업한다.** 다른 도구 인계를 대비할 이유가 없어져 사본과 그 자리에 두었던 위치 안내문까지 모두 삭제했다.

번역본 `PRD.en.md`·`PRD.ja.md`·`PRD.es.md`·`PRD.ar.md`·`PRD.zh-CN.md`·`PRD.zh-TW.md`는 과거 시점의 스냅샷이며 최신이 아니다(§12 사투리 교정, 밀린 작업 분리 등이 빠졌고 폐지된 사본 동기화 관례가 남아 있다). **판단 근거로 쓰지 말 것 — 정본은 이 한국어 `PRD.md`다.** 필요하면 그때 다시 생성한다.

## 세션 인계 노트 (새 세션에서 먼저 읽을 것)

> ### 다음 세션 첫 작업 (2026-08-02 저장, 사용자가 이어서 하기로 함)
>
> 2026-08-02에 `engine.py`·`dictionary.py` 모듈 분할(§46~§48)과 잠복 크래시 수정(§49), 리팩터링 안전망 도구(§50)까지 끝냈다. **커밋은 `9b41cf8`·`2cc75fd`, 푸시는 안 했다.** 다음은 **정확도 자체를 올리는 작업**으로 넘어가기로 했고, 사용자가 아래 둘 중 어디부터 갈지 정한다.
>
> **① held-out 테스트셋 확대** (`docs/BACKLOG.md` 9번)
> - 지금 `examples/eval/heldout.jsonl`은 **14건**뿐이다. 이 규모로는 "고쳤더니 정확도가 올랐다"를 수치로 말할 수 없다 — 한 건이 7.1%p라 노이즈와 개선이 구분되지 않는다.
> - 처음부터 만드는 일이 아니라 **건수를 늘리는 일**이다. 실행기 `examples/eval/run_eval.py`와 라벨 형식은 이미 있다.
> - 라벨은 반드시 사전·규범으로 검증해서 붙인다(`.claude/skills/grammar-rule-verify-then-code`). 추측 라벨을 넣으면 지표 자체가 거짓이 된다.
> - **먼저 하는 것을 권한다** — ②의 효과를 재려면 눈금자가 먼저 있어야 한다.
>
> **② 표제어 조회 중앙화** (`docs/DESIGN_PRINCIPLES.md` 수렴 리팩터링 목표 1)
> - "이 토막을 오류로 판정하기 전에, 그것을 포함하는 최대 사전-유효 구간을 확인" 하는 단일 함수를 만들고 모든 검사가 그것만 호출하게 한다. 원리 1(조각 대조) 오탐 부류가 통째로 사라진다.
> - **착지 지점은 이미 비워 뒀다**: `subtitle_corrector/engine/lexicon.py`. 지금 흩어져 있는 것 — `_covered_by_larger_dictionary_unit`, `_is_reduplication`, `_is_native_compound`, `_tensified_headword_variant`, `_is_productive_demonym_compound`, `_is_verb_stem_mistagged_as_noun`.
> - 이건 **로직 변경**이다. 분할과 달리 출력이 바뀌므로 `tools/diff_behavior.py`의 차이 0건을 기대하면 안 된다. 대신 **바뀐 항목을 한 건씩 사람이 판정**해야 하고, 그래서 ①이 먼저인 편이 낫다.
>
> **구조 변경을 또 하게 되면**: `python tools/check_names.py`(0건이어야 함) + `tools/diff_behavior.py` 대조를 반드시 돌린다(`AGENTS.md` 참고).
>
> 함께 열려 있는 것: `docs/BACKLOG.md` 23번(조사+보조 용언 분리 제안이 조각 사전 확인에 걸려 들쭉날쭉), 22번(`IMPLEMENTATION_LOG.md` 3회차 아카이브 — 약 40KB로 임계 초과).

- **핵심 원칙**: "확률적 추측이 아닌 권위 있는 규범 근거 기반, 애매하면 반드시 사람에게 확인" — 이 원칙에 어긋나는 자동화(예: 문맥 추측으로 판단)는 제안조차 하지 말 것. `docs/KNOWN_LIMITATIONS.md`의 목록 전체가 이 원칙을 지키려다 발견/수정한 버그와 설계 결정의 기록이니, 새 기능을 넣기 전에 먼저 훑어볼 것.
- **사투리 처리 원칙(2026-07-25 재설계)**: 대본 속 사투리는 거의 항상 작가의 의도다. 사투리가 지정된 화자의 기본값은 **protect** — 대사를 완전히 그대로 두고 표준화 교정도 플래그도 하지 않는다. **assist**는 텍스트를 바꾸지 않고 표준어→사투리 제안만 남기고, **to_standard**는 드문 opt-in으로 표준어 자동 변환을 한다. 미지정 화자는 표준어로 간주하되 어미가 표준이 아니어도 자동교정하지 않고, 이름이 있으면 자동 감지 플래그만 남긴다. 의도된 사투리와 오타를 구분하는 것은 판별 불가능한 경계 사례라, 확률적 자동 수정 대신 사람 검토에 맡긴다. 자세한 내용은 §12.
- **지금까지 구현된 것**: 외래어 표기(kornorms, 일반 용어 자동교정/고유명사는 항상 확인 플래그), 맞춤법(표준국어대사전+우리말샘, NNG/VV/VA만 검사), 띄어쓰기(조사·어미 결합은 자동교정, 합성어/의존명사/보조용언은 사전 근거 있으면 자동·없으면 플래그, 보조용언은 원칙대로 띄어쓰기 자동 통일), 확정 오류 패턴·차별적 표현 자동교정, 순화어 플래그. 상세 내역과 각 버그의 발견 경위는 `docs/KNOWN_LIMITATIONS.md`와 `docs/log-archive/2026-h2.md` 참고. **웹 API + 저장(§11, 2026-07-16 구현)**: `subtitle_corrector/api.py`(FastAPI)가 CLI와 같은 엔진(`engine.correct_entries`)을 그대로 재사용해 `.srt`/`.txt`/`.docx` 업로드→교정 결과를 Supabase에 저장, `static/index.html`이 업로드 화면 겸 결과 조회 화면(고유명사·요리명 입력창 포함). 로컬 테스트만 완료, 실제 Supabase/Render 배포는 사용자가 `DEPLOY.md`를 따라 직접 진행해야 함(계정 생성이 필요해 AI가 대신할 수 없음) — **2026-07-22 Render 배포 시도 결과, kiwipiepy 모델 로딩만으로 약 310MB가 들어 무료 티어 512MB 한도를 거의 다 써버려 502 에러 발생, 배포 자체를 보류하기로 함**(`docs/RETROSPECTIVE.md` 참고). 다음 세션에서 배포를 다시 시도할지부터 확인할 것. `docs/log-archive/2026-h2.md` §13 참고: kiwi가 모르는 단어(고유명사·요리명)를 잘못 쪼개던 문제를 `register_custom_words()`(kiwi.add_user_word)로 해결.
- **가장 크게 남은 작업**: **온라인가나다 연동(2단계, 미구현)** — 사전/규범(1단계)으로도 안 풀리는 애매한 표기를 국립국어원 온라인가나다 과거 답변 아카이브로 보완하는 기능. 깔끔한 API가 없는 Q&A 게시판이라 검색/크롤링 방법부터 조사해야 하고, 규모가 꽤 큼(§8 의존성 체크리스트 참고).
- **바로 다음 할 일 ①(2026-07-27 저장, 사용자 지시로 최우선)**: 유사 판례 검색(`subtitle_corrector/precedent_search.py`, 커밋 `24e0ccd`)의 실측 감수. 이 세션에서 구현·테스트(9건)·전체 회귀(150건)까지 끝냈지만, `gananda_precedents.py`의 `PRECEDENTS`가 아직 빈 리스트라 **실제 판례로는 한 번도 돌려보지 못했다**. 그래서 다음 세션 첫 작업은 이것이다:
  1. **2026-08-02 사용자 결정으로 이 항목은 착수 불가로 고정됐다** — 온라인가나다 판례를 미리 훑어 쌓는 작업(`gananda-precedent-research` 예약 배치 포함)을 앞으로 하지 않기로 했다. `PRECEDENTS`를 채울 경로가 없어졌으므로 실측 감수도 할 수 없다. 상세는 `docs/BACKLOG.md` 21번. (아래 2·3번은 판례가 쌓였을 때의 절차로 남겨 둔다.)
  2. 판례가 쌓여 있으면 `pip install -r requirements-optional.txt` 후 실제 모델로 `python -m subtitle_corrector.precedent_search <표현>`을 돌려, 기본 임계값 `min_similarity=0.5`가 적절한지 판단한다. 단어 임베딩은 무관한 표현끼리도 0.3~0.46이 나오므로(2026-07-27 실측: `바다` vs `김치` 0.464, `바다` vs `분노` 0.311) 0.5는 근거 있는 값이 아니라 **잠정치**다. 실제 판례 분포를 보고 조정할 것.
  3. 함께 재검토할 것: 지금은 `expression`만 임베딩한다. 표기가 짧아 문맥이 거의 없으므로, `source`(국립국어원 답변 요지)까지 합쳐 임베딩하면 검색 품질이 오르는지 비교해 볼 가치가 있다.
  - **지킬 선**: 이 기능은 감수자 보조 전용이다. 임계값을 조정하더라도 `word_exists`/`check_precedent` 판정 경로에는 절대 연결하지 않는다(핵심 원칙 위반). `requirements.txt`에 `sentence-transformers`를 넣지도 않는다(Render 512MB 한계).
- **바로 다음 할 일 ②(착수 대기 중, 2026-07-21 저장)**: `docs/log-archive/2026-h2.md` §27 "명사+드리다" 오탐지(부탁드리다 등) 조사·구현 — 아직 시작 안 함, 토큰 소모를 고려해 사용자 요청 시 진행하기로 미뤄둔 상태. 다음 세션에서 이 작업을 이어가려면 §27 전체를 먼저 읽을 것.
- **`docs/KNOWN_LIMITATIONS.md` 참고**: 실사용 중 발견한 심각한 오탐지 3건(차별적 표현 치환이 무관한 단어를 잘못 건드림, 보조용언 띄어쓰기가 사전 등재 복합동사를 강제로 쪼갬, kiwi의 "요" 오태깅)을 전부 패턴 단위로 수정. 앞으로 비슷한 "글자만 보고 치환"·"사전 확인 없이 원칙 강제" 유형의 버그를 새로 넣지 않도록 주의.
- **작은 규모로 더 밀어붙일 수 있는 것**: 제44항 수 표기("몇만/몇백만") 자동교정 승격 — 아래 밀린 작업 목록 4번. (제42항 의존명사 잔여 6개는 검증표는 완료됐지만 **작은 작업이 아니다** — 6개 전부 문맥 규칙이 필요하고 '차'·'들'은 검증표 스스로 난이도가 높다고 적어 두었다. 목록 3번 참고.)
- **작업 방식(반드시 지킬 것)**: 코드만 보고 "됐다"고 하지 말고, 실제 텍스트(뉴스 기사, 블로그 글 등 — 저작권 있는 원문은 스크래치패드에만 두고 리포에는 커밋 안 함)로 `main.py correct`를 돌려서 검증한다. 이 방식으로 실제 버그를 여러 번 잡았음(`docs/IMPLEMENTATION_LOG.md` 각 항목 참고). 회귀 테스트는 `examples/sample.srt`가 기본 픽스처.
- **완료 후 관례**: 기능 하나 끝나면 (1) `examples/sample.srt`로 회귀 확인 → (2) `PRD.md`와 관련 `docs/*.md` 갱신(밀린 작업이 바뀌었으면 `docs/BACKLOG.md`도) — **저장소 밖 사본 동기화는 폐지했다(2026-07-27)** → (3) git commit. 커밋 메시지에 "왜"와 "어떤 버그를 잡았는지"를 구체적으로 남기는 스타일 유지(git log 참고). **git commit은 이 프로젝트에 한해 자동화(2026-07-17, 사용자 지시) — 완료될 때마다 별도 요청 없이 커밋한다. 단, git push는 여기 포함되지 않으며 항상 별도로 명시적 요청을 받은 뒤에만 한다.** (예외: `gananda-precedent-research` 예약 작업처럼 사람이 결과를 검토하기 전인 무인 실행은 그 작업 파일 자체의 지시(커밋 금지)를 따른다.)

## 세션 인계 노트 — 밀린 작업

밀린 작업 목록은 분량이 커지고 자주 바뀌어서 **`docs/BACKLOG.md`**로 분리했다(2026-07-27). **세션을 시작하면 다른 작업보다 먼저 그 파일을 읽고 사용자에게 보고할 것.** 항목을 끝내면 그 파일에서 지우고, 새로 미룬 일이 생기면 그 파일에 추가한다 — 여러 문서에 흩어 놓으면 존재 자체가 잊힌다(그래서 만든 목록이다).

## 1. 배경 및 목적

기존 한국어 자동 교정 툴 '부산대 맞춤법 검사기'를 쓰면서 답답했던 점은 크게 네 가지였다: **오탐지가 지나치게 많았고(정상적인 표현·고유명사까지 오류로 잘못 잡아냄), 왜 틀렸는지 근거 없이 무조건 고치라고 했으며, 그 수정 제안 자체가 무작위로 보일 만큼 일관성이 없어 오히려 그 제안이 새로운 오류처럼 느껴졌고, 한 번에 검사할 수 있는 글자 수에 제한이 있어 긴 문서는 여러 번 나눠 페이지를 넘겨가며 검사해야 했다.**

이 프로젝트는 이 네 가지 문제에 직접 대응한다: 국립국어원의 표준국어대사전·우리말샘·어문 규범을 실시간으로 조회해 판단 근거를 명확히 남기고(플래그 사유에 근거를 항상 명시), 근거가 확실한 항목만 자동으로 고치며, 애매하거나 문맥에 따라 답이 갈리는 항목은 절대 무작위로 고치지 않고 사람에게 확인을 맡기고, 글자 수 제한 없이 문서 파일 하나를 통째로 업로드해 한 번에 처리한다 — 이 중 v1은 **띄어쓰기·맞춤법 교정**에 집중한다.

**목표**: 확률적 추측이 아닌 권위 있는 어문 규범 근거 기반으로, 자동 수정한 항목에 한해 오류 0건을 달성한다. 근거가 불확실한 항목은 절대 함부로 고치지 않고 사람에게 넘긴다.

**대상 사용자**: 전문 번역가. 이들의 반복적인 띄어쓰기·맞춤법 교정 작업 부담을 줄여주는 것이 목적이다. 그래서 오탐지(정상인데 틀렸다고 플래그)와 누락(진짜 오류를 놓침) 둘 다 이 목적에 반한다 — 오탐지는 번역가가 불필요하게 감수 시간을 쓰게 만들고, 누락은 애초에 덜어주려던 수고를 그대로 남긴다. 이 때문에 "확실한 것만 자동 처리, 애매하면 반드시 사람에게 위임"이라는 원칙이 특히 중요하다.

## 2. 범위 (v1)

- 기능: 한국어 텍스트의 **띄어쓰기 교정 + 맞춤법 교정**
- 대상: 일반 텍스트(.txt), MS Word(.docx), 자막(.srt) 등 다양한 형식의 한국어 문서 — 자막 파일도 지원 대상 중 하나일 뿐, 이 도구가 자막 작업만을 위한 것은 아니다
- 대상 언어: **한국어 전용**

### v2로 미룬 항목 (Out of scope)
- 번역 텍스트의 자연스러움·정확성 검증 (원문 대조·번역 스타일 판단이 필요해, 이 도구의 판단 기준인 "권위 있는 규범 근거"만으로는 검증할 방법이 없음 — `docs/log-archive/2026-h2.md` §13 참고)

## 3. 입력 / 출력

- **입력**: 자막(.srt), 일반 텍스트(.txt), MS Word(.docx) 파일
- **출력**:
  - 교정된 결과 파일 (원본과 동일한 포맷 유지 — .srt는 타임코드까지 그대로 보존)
  - 플래그 리포트 파일 (모호해서 자동 수정하지 않은 항목 목록)

## 4. 사용 형태 및 기술 스택

- **CLI 툴**
- 개발 언어: **Python**
- **CLI 프레임워크: Typer**
  - 최소 2개 이상의 서브커맨드(`correct`: 텍스트 교정 실행, `apply-report`: 사용자가 채운 리포트를 원본에 재적용)가 필요한 구조라 argparse보다 서브커맨드 관리가 쉬움
  - 타입 힌트 기반이라 국립국어원 API 응답 등 명확한 데이터 구조를 다루는 이 프로젝트와 궁합이 좋음
  - Click 기반이라 안정성은 유지하면서 보일러플레이트가 적음
  - 향후 pip 배포 시 관례를 그대로 따름

### 아키텍처 원칙 (향후 확장 대비)

교정 로직(사전/규범 조회, 판단, 플래그 생성)은 **CLI와 분리된 순수 라이브러리 모듈**로 설계한다. CLI(Typer)는 이 라이브러리를 호출하는 얇은 인터페이스로만 둔다. (2026-08-02: 그 라이브러리가 3,172줄 한 파일로 커져 `subtitle_corrector/engine/` 패키지 16개 모듈로 나눴다 — 계층과 규칙은 `AGENTS.md`, 경위는 `docs/IMPLEMENTATION_LOG.md` §46.) 이렇게 하면 §9의 백엔드 확장 시 동일한 교정 엔진을 API 서버에서 그대로 재사용할 수 있다 (CLI 코드를 뜯어고칠 필요 없음). v1 시점에 백엔드를 직접 구현하지는 않는다 — 지금 만드는 건 어디까지나 로컬 CLI 도구다.

## 5. 교정 판단 엔진 (핵심 설계)

모호한 문장을 만났을 때 아래 순서로 판단하며, 마지막까지 확신할 수 없으면 **자동 수정하지 않고 그대로 둔 채 플래그**한다 (잘못된 수정이 생기는 것보다 미수정 상태가 낫다는 원칙).

1. **1차 판단 — 규범/사전 근거**
   - 표준국어대사전, 우리말샘 (국립국어원 Open API, `search.do`) — 표제어 존재 여부 확인
   - 표준국어대사전 `search.do` 응답의 `word`/`pos` 필드로 합성어(하이픈 표기, 품사 있음 → 무조건 붙여씀) vs 명사구(캐럿 표기, `pos: "품사 없음"` → 띄어쓰기 원칙·붙임 허용) 구분. kiwi.space()가 놓치는 합성어(예: "노천 카페" → 안 고침)를 사전 근거로 보완해 자동 교정 (`compound_status()`, `correct_compound_spacing()`, 구현 완료)
   - 표준국어대사전 사전 내용 API(`view.do`) — 용례, 일부 단어에 붙은 한글 맞춤법 근거(`norm_info`) 확인 (아직 미구현)
   - 한국어 어문 규범 오픈API(`kornorms/exampleReqList.do`) — 국립국어원이 이미 공식 확정한 외래어·로마자 표기 용례(한글표기 ↔ 원어표기, 인명/지명/일반용어) 조회. 새 API 키 필요
   - 외래어 표기법 규정 원문(국립국어원 어문규범 누리집, 표기 기본 원칙 5개 조항 + 언어별 표기 세칙) — API가 없는 정적 문서. kornorms API에도 없는 완전히 새로운 외래어(아직 공식 표기가 없는 신조어)를 다룰 때만 참고하는 최후 수단. 이마저 애매하면 3차로 넘김
2. **2차 판단 — 온라인가나다 답변 아카이브**
   - 온라인가나다는 실시간 API가 아니라 사람이 직접 답변하는 게시판이므로, 실시간 질의는 하지 않는다
   - 대신 **기존에 공개된 과거 답변 아카이브를 검색/참고**하는 방식으로만 활용 (축적된 판례는 `subtitle_corrector/gananda_precedents.py`, 아직 코드로 안 옮긴 백로그 문법은 `docs/GRAMMAR_PRECEDENTS_TABLE.md`)
3. **3차 — 그래도 모호하면 사람에게 위임**
   - 자동 수정 금지, 리포트에 플래그

### 핵심 원칙: "실시간 사전 데이터가 항상 우선"

표준국어대사전/우리말샘 API로 실시간 조회한 결과가, 코드에 하드코딩된 가정이나 과거에 학습한 교육자료 내용보다 항상 우선한다. 규정·사전은 개정되므로, "예전에 확인했으니 맞다"고 코드에 고정값으로 박아두면 안 되고, 판단이 필요한 순간마다 다시 조회한다. 이 원칙이 실전에서 왜 필요했는지(하드코딩된 가정이 실제로 낡아서 버그가 됐던 사례)는 `docs/log-archive/2026-h2.md` §17 "하드코딩된 검증 가정을 실시간 조회로 교체" 참고.

### 작업 원칙: "검증표 먼저, 코드는 나중" (2026-07-17 확정)

`correct_aux_verb_spacing()` 패턴 2("할만하다"류)에서 코드를 먼저 짜고 발견되는 예외를 그때그때 패치하는 방식으로 진행하다가, 사용자가 실제 우리말샘 용례로 지적한 오류("할만하다"를 "할 만 하다"로 잘못 고침)를 두 번이나 겪었다. 원인은 "규칙이 이럴 것이다"라는 가정으로 먼저 코드를 짜고 나서 사전으로 검증한 순서 자체에 있었다.

**앞으로 규칙(특히 예외가 있을 수 있는 문법 패턴)을 새로 추가할 때는 반드시 이 순서를 지킨다** (구체적 절차는 `.claude/skills/grammar-rule-verify-then-code/SKILL.md` 참고):
1. 관련된 모든 경우의 수(해당 패턴에 들어갈 수 있는 표현 전부, 위 §5-1/2/3처럼 표준국어대사전·우리말샘·kornorms·온라인가나다 판례까지)를 먼저 사전 API로 직접 조회해 **정답표(어떤 입력 → 어떤 정답)를 만든다.**
2. 그 정답표에 정확히 맞는 코드를 짠다 — 코드가 표를 따라가는 것이지, 표가 코드를 사후 검증하는 게 아니다.
3. 구현 후에도 표에 없던 새 사례가 발견되면, 코드를 임시로 패치하지 말고 그 사례를 먼저 표에 추가(사전 재조회)한 뒤 표에 맞춰 코드를 고친다.

### 알려진 한계 / 유지보수 요구사항

실사용 중 발견한 세부 오탐지·버그와 그 수정 원칙, 아직 미구현인 한계의 전체 목록은 `docs/KNOWN_LIMITATIONS.md`로 옮겼다 — 새 기능을 넣기 전에 반드시 먼저 훑어볼 것.

## 6. 플래그 & 재적용 워크플로우 (구현 완료 — `correct` / `apply-report`)

1. 자막 파일 전체를 처리
2. 확신 가능한 항목은 즉시 자동 교정
3. 모호한 항목은 실행 중 끊기지 않고, 전체 처리가 끝난 뒤 **리포트 파일 하나로 모아서 출력** (라인 번호 + 텍스트 + 참고 근거). 인명·지명처럼 자동 적용은 됐지만 더블체크가 필요한 항목은, 이미 적용된 결과 텍스트와 함께 무엇이 왜 바뀌었는지 참고 근거를 리포트에 남긴다.
4. 사용자가 리포트 파일에 직접 수정값을 입력
5. 프로그램을 다시 실행하면 사용자가 입력한 수정값이 **원본 자막 파일에 자동 반영**

## 7. 완료 기준 / 검증 방법

- **검증 방식**: 사람이 정답을 달아놓은 테스트셋 대비 정확도 측정
  - 목표: 프로그램이 자동으로(플래그 없이) 수정한 항목 중 오류 0건
- **테스트셋 출처**: 직접 수집/제작 (실제 유튜브 영상·STT 결과물을 모아 정답 라벨링)

## 8. 의존성 / 선행 작업

- [x] 국립국어원 Open API 키 신청 (표준국어대사전, 우리말샘)
- [x] 한국어 어문 규범 Open API 키 신청 (kornorms, 외래어·로마자 표기 용례)
- [x] 온용어(K-term) Open API 키 신청 (다듬은 말/표준 전문용어 조회, 2026-07-24)
- [x] 한국어기초사전 Open API 키 신청 (초급자 대상 사전, 2026-07-24)
- [x] 지역어 종합 정보 Open API 키 신청 (방언↔표준어, 함수만 추가, 파이프라인 미연결, 2026-07-24)
- [x] 온라인가나다 판례 축적 — 전체 크롤링 대신, 실제로 쓰이는 사례를 배치로 조사해 쌓는 방식으로 구현 (`docs/log-archive/2026-h2.md` §17 참고)
- [~] 테스트셋 수집 및 정답 라벨링 — **부분 완료(2026-07-27 확인)**: `examples/eval/heldout.jsonl`에 정답 라벨 14건(id/category/input/gold/basis/verify)과 실행기 `examples/eval/run_eval.py`가 이미 있다. 남은 일은 새로 만드는 것이 아니라 **건수를 늘리는 것**이다.

## 9. 향후 확장 로드맵 (v1 이후)

지금 만드는 것은 로컬 CLI 도구이지만, 나중에 서비스 형태(웹/백엔드)로 확장할 가능성을 고려해 아래 항목을 로드맵으로 남겨둔다. v1 설계에는 §4의 "아키텍처 원칙"(교정 엔진과 인터페이스 분리)만 반영하고, 아래 기능 자체는 만들지 않는다.

- **로그인/계정**: 사용자별 처리 이력 관리를 위해 필요. 인증 방식(자체 구현 vs OAuth 등)은 미정. 아직 미구현 — §11의 저장은 로그인 없이 "id를 아는 사람은 누구나 그 결과를 볼 수 있다"는 수준(짧은 링크 공유 정도의 보안)이다. 여러 사용자를 구분해서 관리하려면 이 항목이 선행되어야 함.
- **저장 — 구현 완료 (2026-07-16, §11 참고)**: 업로드한 문서의 교정 결과와 플래그 리포트를 Supabase(Postgres)에 저장. 저장 기간·개인정보(문서 내용) 처리 정책은 아직 정하지 않음(무기한 보관 중) — 실 서비스로 나갈 경우 반드시 결정 필요.
- **결제**: 유료 플랜/사용량 과금 여부. 어떤 기능을 유료화할지(예: 처리량, 맞춤법 API 호출 횟수)는 미정
- **도메인별 전문용어 사전**: 의학 용어 사전(KMLE) 등 특정 분야(의학·법률 등) 전문용어 교정. 국립국어원 공식 오픈API가 아닌 민간 사이트라 이용약관 확인이 선행되어야 하고, 일반 한국어 텍스트라는 v1 범위를 벗어나므로 v2 이후로 미룸
- 이 로드맵이 구체화되면 별도 PRD로 분리해서 다룬다 (백엔드 아키텍처, 인증, 결제 연동은 이 문서의 범위를 벗어남)

## 10. TBD (추후 결정 필요)

- 개발 일정 / 목표 기간
- 배포 방식 (pip 패키지 등)
- 리포트 파일 정확한 포맷 스펙 (json/csv 등)
- 처리 속도 등 성능 목표
- 세부 모듈 아키텍처, 에러 핸들링 정책

## 11. 웹 API / 저장 (구현 완료, 2026-07-16)

수업 과제("내 서비스에 백엔드 붙이기")로 §9의 "저장" 항목을 실제로 구현했다. 새 교정 로직은 추가하지 않고, §4의 아키텍처 원칙대로 기존 엔진을 그대로 호출만 한다.

- **구성**: `subtitle_corrector/api.py`(FastAPI) — `POST /api/correct`(`.srt` 업로드 → `parsers.parse_srt` → `engine.correct_entries` → 결과 저장) / `GET /api/reports/{id}`(저장된 결과 재조회). `static/index.html`이 업로드 화면 겸 결과 화면(`?id=`로 새로고침·다른 기기에서도 결과 유지).
- **저장소**: Supabase(Postgres), `subtitle_corrector/store.py`가 REST(PostgREST)를 `requests`로 직접 호출. 테이블 스키마는 `supabase_schema.sql`.
- **보안 설계 — 이 프로젝트가 선택한 구조는 강의 자료의 "브라우저가 Supabase에 직접 접근" 패턴이 아니다**: 브라우저는 이 FastAPI 서버에만 요청하고, Supabase에는 절대 직접 접근하지 않는다. 따라서 `SUPABASE_SERVICE_KEY`(관리자 키, RLS 무시)를 서버 쪽 환경변수로만 갖고 있어도 안전하며, `reports` 테이블은 RLS를 켜두고 정책을 하나도 만들지 않아 anon(공개) 키로는 어떤 요청도 거부된다 — 서버가 유일한 접근 경로.
- **비밀값**: `STDICT_API_KEY`/`OPENDICT_API_KEY`/`KORNORMS_API_KEY`(기존)+`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`(신규), 전부 `.env`(로컬)/Render 환경변수(배포)로만 존재. `.env.example` 참고.
- **배포**: Render(무료 웹서비스)에 FastAPI 그대로 배포 시도. 계정 생성·대시보드 설정은 사용자가 직접 해야 해서(AI가 대신 로그인 불가) `DEPLOY.md`에 단계별로 정리해둠. **2026-07-22 실제 배포 시도 결과, kiwipiepy 모델 메모리 사용량(약 310MB)이 Render 무료 티어 512MB 한도를 거의 다 써버려 502 에러 발생 — 이 배포 시도 자체를 보류함**(`docs/RETROSPECTIVE.md` 참고). 로컬 테스트(스텁 저장소로 엔진 연동 검증) 및 실제 Supabase 연동(2026-07-16, 사용자가 직접 계정 생성)까지는 완료.
  - **2026-08-02 결정: 클라우드 배포를 하지 않는다(종결).** GitHub에 커밋·푸시만 하고 실행은 로컬에서 한다. 같은 날 재확인 결과 남아 있던 Render 주소는 `GET /` 200(정적 HTML이라 메모리 안 씀) / `POST /api/correct` **502**로, 2026-07-22 진단이 그대로다. 설정 문제가 아니라 자원 한계이며 지연 로딩으로도 해결되지 않는다(기동만 통과, 첫 교정에서 같은 한도). 그 주소는 옛 커밋으로 떠 있어 최신 기능도 반영돼 있지 않으므로 **동작하는 데모로 안내하지 말 것.** 상세는 `docs/BACKLOG.md` 8번.
- **테스트**: `examples/sample.srt`로 FastAPI 라우트(200/404, 파일 형식 검증)와 엔진 연동을 확인함(`store.save_report`/`get_report`를 스텁으로 교체해 실제 Supabase 계정 없이 검증).
- **웹 API/저장 관련 실사용 버그 4건, kiwi 미등록 단어 오탐지, 보안/SQL 종합 검토, 저장 실패 격리 등**: 상세 경위는 `docs/log-archive/2026-h2.md`(§11 관련 항목, §13, §24, §25) 참고.

## 12. 사투리 교정 (구현 완료, 2026-07-24 / 재설계 2026-07-25)

**핵심 원칙(2026-07-25 재설계)**: 대본에 쓰인 사투리는 거의 항상 작가의 **의도**다. 따라서 기본적으로 사투리를 표준어로 바꾸지 않으며, 오류로 플래그하지도 않는다. "의도된 사투리"와 "오타"를 확실히 구분하는 것은 판별 불가능한 경계 사례이므로, 확률적 추정(heuristic)으로 자동 수정하지 않고 사람 검토에 맡긴다(§NNP 안전 정책과 동일한 원칙).

- **화자별 사투리 설정**: 사용자가 각 화자에게 사투리 지역을 지정할 수 있다(경상도·제주도·전라도·충청도). SDH 브래킷([이름])에서 화자를 추출한다.
- **지정된 화자의 처리 모드**(`dialect_modes`, 화자별):
  - **protect** — 지정된 화자의 **기본값**. 대사를 완전히 그대로 둔다. 표준화 자동 교정(외래어·조사/합성어/보조용언 띄어쓰기·항상 틀린 표현·비표준어·차별어)도, 인명/고유명사 검토 플래그도, 맞춤법·순화어·띄어쓰기 검사도 **전부 건너뛴다**. 사투리 플래그도 남기지 않는다. (감수하는 트레이드오프: 보호된 대사 안의 진짜 오타도 그대로 둔다 — 사투리와 오타를 신뢰성 있게 구분할 수 없기 때문이며, 이런 경계 사례에 확률적 자동 수정을 만들지 않는 것이 프로젝트 정책이다.)
  - **assist** — 작가가 사투리 화자를 원하지만 사투리에 익숙하지 않을 때. 텍스트를 자동으로 바꾸지 않고 표준화 파이프라인도 돌리지 않는다(표준화는 의도와 정반대). 대신 표준어→사투리 형태를 제안하는 플래그(suggested_fix)만 남긴다. `convert_dialect(text, region, "to_dialect")`가 바꿀 게 없고 `search_dialect()`도 비면 플래그를 남기지 않는다. 채택 여부는 사람이 리포트에서 결정한다.
  - **to_standard** — 드문 opt-in. `convert_dialect(text, region, "to_standard")`로 사투리→표준어를 자동 적용하고 확인 플래그를 남긴다. 이 화자는 표준 출력을 원하므로, 변환된 텍스트에 일반 표준화 파이프라인도 적용한다.
  - **하위 호환**: 옛 모드명 `flag_only`→`protect`, `to_dialect`→`assist`로 매핑한다(옛 호출부 호환). 옛 자동 재작성 `to_dialect`(표준어→사투리 자동 반영)와 옛 기본값 `flag_only`(사투리를 의심스러운 것으로 플래그)는 제거됐다.
- **문서 전체 사투리 설정(2026-08-02 추가)**: 화자별 지정이 없는 줄에 적용되는 문서 단위 (지역, 모드). 소설처럼 글 전체가 한 사투리인 문서를 다루기 위한 것이다(예: 박경리 '토지'를 현대 표준어로). **일반 글에는 화자 표기 자체가 없어서, 이 설정이 없으면 사투리 기능이 아예 걸리지 않았다.** 화자별 지정이 있으면 그쪽이 우선한다(`resolve_dialect_mode`의 fallback). 모드 기본값은 여전히 protect — 글 전체를 말없이 표준어로 바꿔 버리면 안 되기 때문이다. 노출: `correct_entries(dialect_region=, dialect_mode=)`, `/api/correct`의 `dialect_region`·`dialect_mode` Form(지원하지 않는 지역명은 무시), `main.py --dialect-region/--dialect-mode`, 웹 UI의 "문서 전체" 드롭다운.
  - **한계**: 변환 품질은 `convert_dialect()`의 정적 매핑에 달려 있고 지역어 API는 서버 장애로 멈춰 있다(`docs/BACKLOG.md` 5번). 실측(2026-08-02): "이거 아이가 마이시 좋다" -> "이거 그래 많이 좋다"는 되지만 "밥은 뭇나 안 뭇나"는 그대로다. **문학 사투리 전면 표준어화는 아직 부분적으로만 된다.**
- **미지정 화자**: 기존 동작 유지. 표준화 파이프라인을 돌리고, 이름이 있으면 자동으로 사투리 패턴을 감지해(비율 >= 0.15) 플래그만 남긴다(사투리 지정을 권유). 자동교정은 하지 않는다.
- **구현**: `dictionary.py`(DIALECT_MARKERS, detect_dialect_ratio, convert_dialect, search_dialect), `engine/options.py`(normalize_dialect_mode, resolve_dialect_mode)·`engine/dialect.py`(check_dialect)·`engine/pipeline.py`(correct_entries에서 모드 우선 게이팅), `api.py`(/api/speakers, /api/dialect-regions, dialect_map·dialect_modes 파라미터, 모드 검증/정규화), `static/index.html`(화자별 지역·모드 드롭다운). 테스트: `tests/test_dialect_modes.py`.
- **알려진 한계**: 지역어 종합 정보 API(dialect.korean.go.kr)가 현재 JSON이 아닌 응답을 돌려주어 `search_dialect()`는 사실상 빈 결과를 반환한다. assist 제안은 정적 `convert_dialect` 매핑에 의존한다.

## 13. 띄어쓰기 기준 선택 — 원칙 / 허용 (구현 완료, 2026-08-02)

**배경**: 한글 맞춤법 제47항은 보조 용언을 "띄어 씀을 원칙으로 하되, 붙여 씀도 허용"한다. 둘 다 맞는 표기이므로 어느 쪽을 쓸지는 규범이 아니라 **작품의 선택**이다. 다만 한 작품 안에서 두 기준이 섞이면 그 자체가 교정 대상이므로, 사용자가 기준을 고르면 문서 전체가 그 한쪽으로 통일되어야 한다. 기존에는 원칙(띄어 씀)만 지원했다.

- **기준값**(`spacing_mode`, 문서 단위 — 줄마다 다르게 줄 수 없다):
  - **principle** — 기본값. 붙여 쓴 보조 용언을 띄어 쓴다(`할만하다` -> `할 만하다`). 기존 동작과 동일하다.
  - **allowance** — 띄어 쓴 보조 용언을 붙여 쓴다(`해 보자` -> `해보자`, `올 듯하다` -> `올듯하다`).
  - 모르는 값·빈 값은 `principle`로 정규화한다(`normalize_spacing_mode`). 규범 기본값이 아닌 쪽으로 문서 전체가 조용히 바뀌면 안 되기 때문이다.
- **붙임이 불가능한 구간**은 allowance에서도 띄어 쓴다. 이건 기준 혼용이 아니라 제47항 자체의 예외이므로, 사용자가 오해하지 않도록 자동 교정 로그에 `[붙임 불가]` 사유를 남긴다(사용자 결정, 2026-08-02).
  - 본용언이 3음절 이상 사전 등재 합성어(`덤벼들어 보아라`) — 제47항 단서상 항상 띄어 쓴다.
  - 의존명사+하다/싶다 조합이 사전에 없는 경우(`만싶다`) — 붙임 근거가 없어 그대로 둔다.
- **자동 통일의 적용 범위는 제47항 보조 용언뿐이다**(사용자 결정, 2026-08-02). 제49항(고유명사)·제50항(전문 용어)은 자동 통일 대신 혼용 감지로 처리한다 — §14 참고. 제46항(단음절 단어 연속)은 미착수다.
  - **정정(2026-08-02)**: 이 절에 처음 "제50항은 판정 로직 자체가 없다"고 적었는데 부정확했다. `_TERM_COMPOUND_TAGS`(`engine/kiwi_adapter.py`)가 이미 있고, 붙여 쓴 전문 용어·고유명사를 kiwi가 쪼개자고 제안할 때 되돌려 **보호**한다. 없는 것은 "선택한 기준으로 통일하는" 로직이다.
- **구현**: `engine/options.py`(`SPACING_MODES`, `normalize_spacing_mode`)·`engine/spacing.py`(`_aux_verb_spacing(text, mode)` — 기존 `correct_aux_verb_spacing`은 원칙 전용 얇은 래퍼로 남김, `correct_entries(spacing_mode=...)`), `api.py`(`/api/correct`의 `spacing_mode` Form), `main.py`(`--spacing principle|allowance`), `static/index.html`(띄어쓰기 기준 라디오). 테스트: `tests/test_engine.py::TestAuxVerbSpacingAllowanceMode`(10건).
- **검증**(2026-08-02): 전체 스위트 160건 통과. `examples/sample.srt` 회귀 — 두 기준의 결과 차이는 `그 일은 할 만하다`/`그 일은 할만하다` 한 줄뿐이고 나머지 자동교정·플래그 3건은 동일. 허용 기준으로 붙인 형태가 `check_spacing`에서 다시 "확인 필요"로 뜨지 않는 것도 확인(`_normalize_aux_verb_spacing`이 원문 기준으로 kiwi 제안을 되돌리므로 기준과 무관하게 동작한다).
- **번역본 PRD 미반영**: `PRD.en.md` 등 6개 언어 사본에는 이 절이 없다.

## 14. 고유명사·전문 용어 띄어쓰기 혼용 감지 (구현 완료, 2026-08-02)

**규칙**(사용자 정리, 2026-08-02):

| 대상 | 원칙 | 허용 |
|---|---|---|
| 고유명사(제49항) | 다 띄어 씀 — `마포 경찰서 강력 팀` | **의미 단위별** 붙임 — `마포경찰서 강력팀` (부분 붙임 가능) |
| 전문 용어(제50항) | 다 띄어 씀 — `만성 골수성 백혈병` | **전부** 붙임 — `만성골수성백혈병` (부분만 붙이는 것 불가) |
| 거리 이름 | `대로/로/가`는 이름에 **붙여 씀**(`세종대로`, `충무로`, `종로2가`) / `길·거리`는 띄어 씀(`개나리 길`) | `길·거리`도 고유명사로 보아 전부 붙임 허용(`개나리길`) |

**처리 방식 — 자동 통일이 아니라 혼용 감지 플래그**(사용자 결정, 2026-08-02). 원칙·허용 둘 다 맞는 표기이므로 어느 쪽도 오류가 아니고, 오류인 것은 한 작품 안에서 두 표기가 섞이는 것이다. 제47항처럼 자동 통일하지 않는 이유는 붙이려면 **어디까지가 한 용어인지**(고유명사는 의미 단위, 전문 용어는 전체) 알아야 하는데 그 경계가 사전에 없고 문맥 추측이 필요하기 때문이다 — §5의 "확률적 추측으로 자동 교정하지 않는다"에 정면으로 걸린다.

- **판정**: 공백을 지운 문자열이 같은데 표면형이 다른 표기가 한 문서에 2개 이상 나오면 플래그. 문자열 비교만 하므로 추측이 없다.
- **후보 구간**: `_TERM_RUN_TAGS`(= `_TERM_COMPOUND_TAGS` + 어근 XR + 명사 파생 접미사 XSN) 토큰이 공백만 사이에 두고 이어지는 구간과 그 부분 구간. 공백 제외 4~30자, 최대 6토큰. 부분 구간까지 내는 이유는 `만성 골수성 백혈병 진단`과 `만성골수성백혈병 치료`처럼 뒤에 붙는 말이 다르면 최대 구간만으로는 짝지어지지 않기 때문이다. 같은 혼용이 여러 길이로 걸리면 가장 긴 것만 남긴다.
- **의존명사(NNB) 제외**: `말한 대로`/`말한대로`는 제42항이 항상 띄어 쓰라고 정한 별개 규칙이라, 여기 섞으면 정당한 오류를 통일 문제로 오인하게 된다.
- **통일 후보(`suggested_fix`) 우선순위**: 거리 이름 규칙을 어기지 않는 표기 → 문서에서 더 자주 쓴 표기 → 동률이면 원칙(공백이 많은 형태) → 사전순. 거리 이름 규칙을 넣은 이유는, `세종대로`/`세종 대로`가 1회씩일 때 단순 다수결로는 규정에 어긋나는 `세종 대로`를 제안하게 되기 때문이다.
- **하지 않는 것**: `부분만 붙임` 자체를 오류로 판정하지 않는다(전문 용어에서는 금지, 고유명사에서는 허용이라 그 표기가 어느 쪽인지 알아야 갈리는데, 그 구분이 곧 추측이다). 거리 이름의 `대로/로/가`를 띄어 쓴 표기도 그 자체로는 플래그하지 않고 통일 후보 선택에만 쓴다.
- **구현**: `engine/consistency.py`(`_term_runs`, `_violates_street_name_rule`, `check_term_spacing_consistency` — 줄 단위 파이프라인이 끝난 뒤 문서 전체를 한 번 훑는다), 사투리 protect 화자의 대사는 제외. 테스트: `tests/test_engine.py::TestTermSpacingConsistency`(8건).
- **검증**(2026-08-02): 전체 스위트 168건 통과. `examples/sample.srt` 결과·리포트 모두 이전과 완전히 동일(새 플래그로 인한 노이즈 없음).

## 16. 자막 편집 표지 설정 (구현 완료, 2026-08-02)

자막 원고에는 어문 규범의 대상이 아닌 **기술적 표지**가 섞인다. 업계 공통 규칙이 없어 작업자·편집기마다 값이 달라서, 값을 고정하지 않고 **그때그때 설정으로 받아** 교정에서 제외한다(사용자 지정).

| 표지 | 예 | 처리 |
|---|---|---|
| 화면자막 표기 | `"…"`, `@…` | 짝이 있는 문자면 감싸인 **구간만**, 짝이 없는 문자면 **그 줄 전체**를 제외 |
| 줄바꿈 표기 | `\|` | 교정할 때만 **실제 줄바꿈으로 취급**하고, 결과에는 표지를 그대로 되돌림 |
| 자막 위치 표기 | `{\an8}` | 제어 코드라 통째로 보호 |

- **짝 판단은 형태로 자동**(`_MARKER_PAIRS`: 따옴표·괄호류). 사용자는 문자만 적고 방식을 고를 필요가 없다. 여는 표지를 닫지 않은 경우는 줄 끝까지 보호한다 — 그 뒤는 화면자막일 가능성이 높고, 교정하지 않는 쪽이 덜 위험하다.
- **제외 강도는 완전 제외**: 보호 구간에는 자동 교정도 확인 플래그도 없다(사투리 protect 모드와 같은 방식). 화면에 뜬 글자를 그대로 옮긴 것이라 규범 적용 대상이 아니기 때문이다.
- **줄바꿈 표지를 실제 줄바꿈으로 보는 이유**: 그래야 "각 줄 끝 마침표 생략"(§15 구두점 규칙)이 화면에 보이는 줄과 같은 기준으로 적용된다. 표지만 보호하면 `안녕하세요.|반갑습니다.`의 앞 마침표가 줄 끝으로 안 잡힌다.
- **자막 모드에서만 적용**한다. 일반 글에는 이런 편집 표지가 없다.
- **구조**: 이 기능 때문에 한 줄을 구간 단위로 교정할 수 있어야 해서, `correct_entries()` 안에 박혀 있던 줄 단위 파이프라인을 `_correct_line()`으로 **순수 이동**했다(로직 변경 없음, 이동 후 195건 그대로 통과). 표지 처리는 그 위의 `_correct_line_with_markers()`가 맡는다.
- **플래그는 줄 전체 기준으로 되돌린다**: 구간 단위로 교정해도 `original_text`·`suggested_fix`는 줄 전체여야 한다. 조각으로 남으면 리포트에 일부만 보이고, `apply-report`가 그 조각으로 줄 전체를 덮어써 나머지 대사를 지운다.
- **노출**: `correct_entries(markers=)`, `/api/correct`의 `screen_text_marker`·`line_break_marker`·`position_marker` Form, `main.py --screen-text-marker/--line-break-marker/--position-marker`, 웹 UI "자막 편집 표지" 입력 3칸. 테스트: `tests/test_subtitle_markers.py`(13건).

## 17. 자막 읽기 속도(CPS)·한 줄 글자 수 검사 — 제거됨 (2026-08-02)

**사용자 결정으로 삭제했다.** 읽기 속도와 줄당 글자 수는 자막 **편집기**의 기능이지 교정기의 기능이 아니다 — 이 도구는 어문 규범과 자막 표기 관례를 다루고, 시간·배치 제약은 편집기가 다룬다. 나중에 자막 편집기를 만들 때 그쪽으로 옮긴다. 아래는 삭제 전 설계 기록이다.

<details><summary>삭제된 설계(참고용)</summary>

**어문 규범이 아니라 사람이 글을 읽는 속도**라는 물리적 제약이다. 글자 수 상한이 배급사마다 갈리는 것과 달리 이 값은 크게 벌어지지 않는다 — 성인 **17자/초**, 아동물 **13자/초**가 널리 쓰이고 20자/초를 넘으면 사실상 읽을 수 없다고 본다(넷플릭스 등 스트리밍 자막 지침, 자막 품질 연구 공통).

- **계산**: 화면에 보이는 글자 수 ÷ 표시 시간(타임코드 차). `.srt`의 타임코드는 이미 파싱하고 있었으나 지금까지 한 번도 쓰지 않았다.
- **무엇을 세나**: 공백은 센다(화면에서 자리를 차지하고 읽는 시간에 들어간다 — 업계 CPS 계산도 동일). 줄바꿈은 글자가 아니므로 빼고, 줄바꿈 표기(`|`)·자막 위치 표기(`{\an8}`)는 시청자에게 보이지 않는 편집 기호라 뺀다. **화자명·어조 표기는 SDH에서 실제로 화면에 나오므로 센다.**
- **자동 교정하지 않는다**: 해결책은 표현을 줄이거나(번역을 고치는 일) 표시 시간을 늘리는 것(타임코드를 고치는 일)뿐이라 둘 다 사람 몫이다.
- **적용 범위**: 자막 모드 + 타임코드가 있는 항목만. 일반 텍스트·`.docx`에는 타임코드가 없어 조용히 건너뛴다. 0 이하를 주면 검사하지 않는다.
- **구현**: `engine.py`의 `check_reading_speed`(+ `SUBTITLE_MAX_CPS`, `_timecode_seconds`, `_displayed_length`), `correct_entries(max_cps=)`, `/api/correct`의 `max_cps` Form, `main.py --max-cps`, 웹 UI "읽기 속도 상한". 테스트: `tests/test_reading_speed.py`(17건).
- **검증**(2026-08-02): 전체 스위트 242건 통과, held-out 12/14로 기준선 동일.

</details>


## 18. 자막 표시 띄어쓰기 규칙 (구현 완료, 2026-08-02)

자막 위치 표기(`{n8}`)·화자명 표기(`[민수]`)·어조/지문 표기(`(웃으며)`)는 대사가 아니라 **편집·전달용 표시**다. 사용자 지정 규칙은 둘뿐이고, 둘 다 정답이 하나라 자동 교정한다.

1. **표시끼리는 붙여 쓴다.** 연달아 올 때 사이에 공백이 있으면 오류다 — `{n8} [민수]` → `{n8}[민수]`.
2. **표시와 말자막 사이는 정확히 한 칸.** 붙어 있거나 여러 칸이면 오류다 — `[민수]안녕` → `[민수] 안녕`.

- 표시만 있고 뒤에 대사가 없는 줄(효과음 등)은 건드리지 않는다.
- **설정에서 받은 부호만 표시로 본다.** 대괄호만 쓰는 원고에서 괄호까지 표시로 취급하면 정당한 표기를 망친다.
- 위치 표기는 교정 파이프라인에서 보호 조각으로 먼저 떼어지므로, 인접 규칙은 조각을 **합친 뒤에** 한 번 더 적용한다. 그러지 않으면 조각 경계를 사이에 둔 공백(`{n8} [민수]`)을 볼 수 없다.
- 구현: `engine/markers.py`의 `_marker_unit_pattern`·`engine/subtitle_rules.py`의 `correct_subtitle_bracket_spacing`. 테스트: `tests/test_subtitle_markers.py::TestMarkerAdjacency`(6건).
