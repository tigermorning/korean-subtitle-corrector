<!-- lang-switcher:start -->
<p align="center">
  <a href="PRD.en.md">English</a>
  ·
  <a href="PRD.zh-CN.md">中文(简体)</a>
  ·
  <a href="PRD.zh-TW.md">中文(繁體)</a>
  ·
  <a href="PRD.ja.md">日本語</a>
  ·
  <a href="PRD.es.md">Español</a>
  ·
  <a href="PRD.ar.md">العربية</a>
</p>
<!-- lang-switcher:end -->

# 한국어 띄어쓰기·맞춤법 자동 교정 도구 — PRD

> **더 이상 초안이 아니다.** CLI·웹 API·평가셋·자동 교정 관문이 전부 구현돼 있고
> 실사용 감수를 여러 차례 통과했다. CLI와 웹 화면 둘 다 지원하며, 화면 쪽이 실사용
> 경로다.

## 문서 구성

이 PRD에는 **잘 안 바뀌는 핵심 스펙**만 남기고, 계속 쌓이는 실사용 로그·한계
목록·검증표는 별도 파일로 옮긴다.

- **`PRD.md`(이 파일)**: 배경/목적, 범위, 입출력, 기술스택, 교정 판단 엔진의 핵심
  설계, 워크플로우, 완료 기준, 의존성, 로드맵, TBD.
- **`docs/BACKLOG.md`**: 밀린 작업 목록. **세션 시작 시 이 PRD 다음으로 가장 먼저
  읽고 사용자에게 보고한다.**
- **`docs/KNOWN_LIMITATIONS.md`**: 실사용 중 발견한 오탐지/버그와 그 수정 원칙,
  아직 미구현인 한계.
- **`docs/IMPLEMENTATION_LOG.md`**, **`docs/log-archive/2026-h2.md`**: 날짜순 작업
  이력. 커지면 반기별로 아카이브(규칙은 파일 안에 명시).
- **`docs/GRAMMAR_PRECEDENTS_TABLE.md`**: 아직 코드로 구현 안 된 문법 규칙의
  온라인가나다 검증표.
- **`subtitle_corrector/gananda_precedents.py`**: 이미 코드가 다루는 표현의
  온라인가나다 판례(코드와 함께 있어야 함).

구조를 바꾸면 이 목록을 참조하는 곳도 함께 갱신할 것: `.claude/skills/`의 4개
스킬, `AGENTS.md`. (판례 스터디용 예약 스킬 `~/.claude/scheduled-tasks/
gananda-precedent-research/`는 2026-09-02 판례 스터디 영구 종결과 함께
삭제됐다 — 더 이상 존재하지 않으니 참조하지 말 것.)

**PRD 사본을 만들지 않는다(2026-07-27 결정, 이 파일이 유일한 정본).** 과거 다른
도구 인계를 대비해 사본을 동기화하던 관례가 실제 사고(작업이 사본에만 반영되고
저장소엔 며칠간 미반영, 사투리 절 전체가 사본에서 누락)를 냈다 — 지금은 Claude
Code로만 작업하므로 그 이유 자체가 없어졌다.

번역본(`PRD.en.md` 등 6개 언어)은 과거 스냅샷이며 최신이 아니다. **판단 근거로
쓰지 말 것 — 정본은 이 한국어 `PRD.md`뿐이다.** 필요하면 그때 다시 생성한다.

## 세션 인계 노트 (새 세션에서 먼저 읽을 것)

**최신 상태·밀린 작업은 전부 `docs/BACKLOG.md`에 있다.** 날짜순 경위(무엇을 왜
고쳤는지)는 `docs/IMPLEMENTATION_LOG.md`·`docs/log-archive/2026-h2.md`에 있다.
아래는 세션이 바뀌어도 잘 안 변하는 원칙·작업 방식만 남긴다.

- **핵심 원칙**: "확률적 추측이 아닌 권위 있는 규범 근거 기반, 애매하면 반드시
  사람에게 확인" — 이 원칙에 어긋나는 자동화(문맥 추측으로 판단 등)는 제안조차
  하지 말 것. `docs/KNOWN_LIMITATIONS.md`가 이 원칙을 지키려다 발견/수정한
  버그와 설계 결정의 기록이니, 새 기능을 넣기 전에 먼저 훑어볼 것.
- **사투리 처리 원칙**: 지정된 화자의 기본값은 **protect**(원문 그대로, 교정도
  플래그도 안 함). assist·to_standard도 텍스트는 바꾸지 않고 제안만 남긴다.
  미지정 화자는 표준어로 간주하되 어미가 비표준이어도 자동교정하지 않는다 —
  의도된 사투리와 오타를 구분하는 것은 판별 불가능한 경계 사례이기 때문이다.
  상세 규칙은 §12.
- **대상 사용자**: 일반 사용자부터 전문 번역가까지 누구나(§1). 전문 번역가
  전용으로 설계·제한한 적은 없다 — 기준이 엄격한 전문가가 매번 근거를 확인해도
  믿을 만큼 보수적인 판정이, 일반 사용자에게도 그대로 이득이 된다.
- **지금까지 구현된 것**: 외래어 표기(kornorms), 맞춤법(표준국어대사전+우리말샘,
  NNG/VV/VA), 띄어쓰기(조사·어미 자동, 합성어/의존명사/보조용언은 사전 근거
  있으면 자동·없으면 플래그), 확정 오류 패턴·차별적 표현 자동교정, 순화어
  플래그, 웹 API + 저장(§11 — 클라우드 배포는 자원 한계로 보류, 로컬 실행만
  함). 각 버그의 발견 경위는 `docs/KNOWN_LIMITATIONS.md`와 로그 아카이브 참고.
- **지금 착수 가능한 큰 작업이 없다(2026-09-02 확정)**: 온라인가나다 연동
  2단계·유사 판례 검색 실측(1·7번)은 판례를 미리 축적하는 선행 작업을 하지
  않기로 사용자가 **영구 종결**했다(재검토 안 함). 외래어 표기법 세칙 전면
  도입(31번)과 그에 딸린 작업자자료 이미지 62장(4번)은 "이 낱말이 외래어인가"를
  텍스트만으로 가릴 방법이 없어 막혀 있다 — 발음사전 도입 여부와 무관한 별도
  장벽(`docs/BACKLOG.md` 31번 참고). 열려 있는 유일한 항목은 지역어 종합 정보
  API 500 장애(5번, 외부 서버 복구 대기)뿐이다. 새 작업은 사용자에게 먼저 물을 것.
- **작업 방식(반드시 지킬 것)**: 코드만 보고 "됐다"고 하지 말고, 실제 텍스트(뉴스
  기사·블로그 글 등 — 저작권 있는 원문은 스크래치패드에만 두고 저장소에는 커밋
  하지 않음)로 `main.py correct`를 돌려서 검증한다. 이 방식으로 실제 버그를
  여러 번 잡았다. 회귀 테스트는 `examples/sample.srt`가 기본 픽스처.
- **완료 후 관례**: 기능 하나 끝나면 (1) `examples/sample.srt`로 회귀 확인 → (2)
  `PRD.md`와 관련 `docs/*.md` 갱신(밀린 작업이 바뀌었으면 `docs/BACKLOG.md`도) →
  (3) git commit. 커밋 메시지에 "왜"와 "어떤 버그를 잡았는지"를 구체적으로 남기는
  스타일 유지(git log 참고). **git commit은 이 프로젝트에 한해 자동화(사용자
  지시) — 완료될 때마다 별도 요청 없이 커밋한다. git push는 여기 포함되지 않으며
  항상 별도로 명시적 요청을 받은 뒤에만 한다.** (예외: 사람이 결과를 검토하기
  전인 무인 실행은 그 작업 파일 자체의 지시(예: 커밋 금지)를 따른다.)

## 1. 배경 및 목적

기존 한국어 자동 교정 툴 '부산대 맞춤법 검사기'를 쓰면서 답답했던 점은 크게 네
가지였다: **오탐지가 지나치게 많았고(정상적인 표현·고유명사까지 오류로 잘못
잡아냄), 왜 틀렸는지 근거 없이 무조건 고치라고 했으며, 그 수정 제안 자체가
무작위로 보일 만큼 일관성이 없어 오히려 그 제안이 새로운 오류처럼 느껴졌고, 한
번에 검사할 수 있는 글자 수에 제한이 있어 긴 문서는 여러 번 나눠 페이지를
넘겨가며 검사해야 했다.**

이 프로젝트는 이 네 가지 문제에 직접 대응한다: 국립국어원의 표준국어대사전·
우리말샘·어문 규범을 실시간으로 조회해 판단 근거를 명확히 남기고(플래그 사유에
근거를 항상 명시), 근거가 확실한 항목만 자동으로 고치며, 애매하거나 문맥에 따라
답이 갈리는 항목은 절대 무작위로 고치지 않고 사람에게 확인을 맡기고, 글자 수
제한 없이 문서 파일 하나를 통째로 업로드해 한 번에 처리한다 — 이 중 v1은
**띄어쓰기·맞춤법 교정**에 집중한다.

**목표**: 확률적 추측이 아닌 권위 있는 어문 규범 근거 기반으로, 자동 수정한
항목에 한해 오류 0건을 달성한다. 근거가 불확실한 항목은 절대 함부로 고치지 않고
사람에게 넘긴다.

**대상 사용자**: 일반 사용자부터 전문 번역가까지 누구나. 이 도구를 전문 번역가
전용으로 설계·제한한 적은 없다 — 강점은 오히려 그 반대 방향이다: 기준이 엄격한
전문가(전문 번역가 등)가 매번 근거를 확인해도 믿고 쓸 수 있을 만큼 판정이
보수적이라는 것이, 일반 사용자에게도 그대로 이득이 된다. 반복적인 띄어쓰기·
맞춤법 교정 부담을 줄이는 것이 목적이므로, 오탐지(정상인데 틀렸다고 플래그)와
누락(진짜 오류를 놓침) 둘 다 이 목적에 반한다 — 오탐지는 불필요하게 감수 시간을
쓰게 만들고, 누락은 애초에 덜어주려던 수고를 그대로 남긴다. 이 때문에 "확실한
것만 자동 처리, 애매하면 반드시 사람에게 위임"이라는 원칙이 특히 중요하다.

## 2. 범위 (v1)

- 기능: 한국어 텍스트의 **띄어쓰기 교정 + 맞춤법 교정**
- 대상: 일반 텍스트(.txt), MS Word(.docx), 자막(.srt) 등 다양한 형식의 한국어
  문서 — 자막 파일도 지원 대상 중 하나일 뿐, 이 도구가 자막 작업만을 위한 것은
  아니다
- 대상 언어: **한국어 전용**

### v2로 미룬 항목 (Out of scope)
- 번역 텍스트의 자연스러움·정확성 검증 (원문 대조·번역 스타일 판단이 필요해, 이
  도구의 판단 기준인 "권위 있는 규범 근거"만으로는 검증할 방법이 없음 —
  `docs/log-archive/2026-h2.md` §13 참고)

## 3. 입력 / 출력

- **입력**: 자막(.srt), 일반 텍스트(.txt), MS Word(.docx) 파일
- **출력**:
  - 교정된 결과 파일 (원본과 동일한 포맷 유지 — .srt는 타임코드까지 그대로 보존)
  - 플래그 리포트 파일 (모호해서 자동 수정하지 않은 항목 목록)

## 4. 사용 형태 및 기술 스택

- **CLI 툴**
- 개발 언어: **Python**
- **CLI 프레임워크: Typer**
  - 최소 2개 이상의 서브커맨드(`correct`: 텍스트 교정 실행, `apply-report`:
    사용자가 채운 리포트를 원본에 재적용)가 필요한 구조라 argparse보다 서브커맨드
    관리가 쉬움
  - 타입 힌트 기반이라 국립국어원 API 응답 등 명확한 데이터 구조를 다루는 이
    프로젝트와 궁합이 좋음
  - Click 기반이라 안정성은 유지하면서 보일러플레이트가 적음
  - 향후 pip 배포 시 관례를 그대로 따름

### 아키텍처 원칙 (향후 확장 대비)

교정 로직(사전/규범 조회, 판단, 플래그 생성)은 **CLI와 분리된 순수 라이브러리
모듈**로 설계한다. CLI(Typer)는 이 라이브러리를 호출하는 얇은 인터페이스로만
둔다. 지금은 `subtitle_corrector/engine/` 패키지로 나뉘어 있다 — 계층과 규칙은
`AGENTS.md`, 분할 경위는 `docs/log-archive/2026-h2.md` 참고. 이렇게 하면 §9의
백엔드 확장 시 동일한 교정 엔진을 API 서버에서 그대로 재사용할 수 있다 (CLI
코드를 뜯어고칠 필요 없음). v1 시점에 백엔드를 직접 구현하지는 않는다 — 지금
만드는 건 어디까지나 로컬 CLI 도구다.

## 5. 교정 판단 엔진 (핵심 설계)

모호한 문장을 만났을 때 아래 순서로 판단하며, 마지막까지 확신할 수 없으면
**자동 수정하지 않고 그대로 둔 채 플래그**한다 (잘못된 수정이 생기는 것보다
미수정 상태가 낫다는 원칙).

1. **1차 판단 — 규범/사전 근거**
   - 표준국어대사전, 우리말샘 (국립국어원 Open API, `search.do`) — 표제어 존재
     여부 확인
   - 표준국어대사전 `search.do` 응답의 `word`/`pos` 필드로 합성어(하이픈 표기,
     품사 있음 → 무조건 붙여씀) vs 명사구(캐럿 표기, `pos: "품사 없음"` →
     띄어쓰기 원칙·붙임 허용) 구분. kiwi.space()가 놓치는 합성어(예: "노천
     카페" → 안 고침)를 사전 근거로 보완해 자동 교정 (`compound_status()`,
     `correct_compound_spacing()`, 구현 완료)
   - 표준국어대사전 사전 내용 API(`view.do`) — 용례, 일부 단어에 붙은 한글
     맞춤법 근거(`norm_info`) 확인 (아직 미구현)
   - 한국어 어문 규범 오픈API(`kornorms/exampleReqList.do`) — 국립국어원이 이미
     공식 확정한 외래어·로마자 표기 용례(한글표기 ↔ 원어표기, 인명/지명/일반용어)
     조회. 새 API 키 필요
   - 외래어 표기법 규정 원문(국립국어원 어문규범 누리집, 표기 기본 원칙 5개
     조항 + 언어별 표기 세칙) — API가 없는 정적 문서. kornorms API에도 없는
     완전히 새로운 외래어(아직 공식 표기가 없는 신조어)를 다룰 때만 참고하는
     최후 수단. 이마저 애매하면 3차로 넘김
2. **2차 판단 — 온라인가나다 답변 아카이브**
   - 온라인가나다는 실시간 API가 아니라 사람이 직접 답변하는 게시판이므로,
     실시간 질의는 하지 않는다
   - 대신 **기존에 공개된 과거 답변 아카이브를 검색/참고**하는 방식으로만 활용
     (축적된 판례는 `subtitle_corrector/gananda_precedents.py`, 아직 코드로 안
     옮긴 백로그 문법은 `docs/GRAMMAR_PRECEDENTS_TABLE.md`)
3. **3차 — 그래도 모호하면 사람에게 위임**
   - 자동 수정 금지, 리포트에 플래그

### 핵심 원칙: "실시간 사전 데이터가 항상 우선"

표준국어대사전/우리말샘 API로 실시간 조회한 결과가, 코드에 하드코딩된 가정이나
과거에 학습한 교육자료 내용보다 항상 우선한다. 규정·사전은 개정되므로, "예전에
확인했으니 맞다"고 코드에 고정값으로 박아두면 안 되고, 판단이 필요한 순간마다
다시 조회한다. 이 원칙이 실전에서 왜 필요했는지는 `docs/log-archive/2026-h2.md`
§17 "하드코딩된 검증 가정을 실시간 조회로 교체" 참고.

### 작업 원칙: "검증표 먼저, 코드는 나중"

`correct_aux_verb_spacing()`에서 코드를 먼저 짜고 발견되는 예외를 그때그때
패치하는 방식으로 진행하다가, 실제 우리말샘 용례와 어긋나는 오류("할만하다"를
"할 만 하다"로 잘못 고침)를 두 번이나 겪었다. 원인은 "규칙이 이럴 것이다"라는
가정으로 먼저 코드를 짜고 나서 사전으로 검증한 순서 자체에 있었다.

**앞으로 규칙(특히 예외가 있을 수 있는 문법 패턴)을 새로 추가할 때는 반드시 이
순서를 지킨다** (구체적 절차는
`.claude/skills/grammar-rule-verify-then-code/SKILL.md` 참고):
1. 관련된 모든 경우의 수(해당 패턴에 들어갈 수 있는 표현 전부, 위 §5-1/2/3처럼
   표준국어대사전·우리말샘·kornorms·온라인가나다 판례까지)를 먼저 사전 API로
   직접 조회해 **정답표(어떤 입력 → 어떤 정답)를 만든다.**
2. 그 정답표에 정확히 맞는 코드를 짠다 — 코드가 표를 따라가는 것이지, 표가
   코드를 사후 검증하는 게 아니다.
3. 구현 후에도 표에 없던 새 사례가 발견되면, 코드를 임시로 패치하지 말고 그
   사례를 먼저 표에 추가(사전 재조회)한 뒤 표에 맞춰 코드를 고친다.

### 알려진 한계 / 유지보수 요구사항

실사용 중 발견한 세부 오탐지·버그와 그 수정 원칙, 아직 미구현인 한계의 전체
목록은 `docs/KNOWN_LIMITATIONS.md`로 옮겼다 — 새 기능을 넣기 전에 반드시 먼저
훑어볼 것.

## 6. 플래그 & 재적용 워크플로우 (구현 완료 — `correct` / `apply-report`)

1. 자막 파일 전체를 처리
2. 확신 가능한 항목은 즉시 자동 교정
3. 모호한 항목은 실행 중 끊기지 않고, 전체 처리가 끝난 뒤 **리포트 파일 하나로
   모아서 출력** (라인 번호 + 텍스트 + 참고 근거). 인명·지명처럼 자동 적용은
   됐지만 더블체크가 필요한 항목은, 이미 적용된 결과 텍스트와 함께 무엇이 왜
   바뀌었는지 참고 근거를 리포트에 남긴다.
4. 사용자가 리포트 파일에 직접 수정값을 입력
5. 프로그램을 다시 실행하면 사용자가 입력한 수정값이 **원본 자막 파일에 자동
   반영**

## 7. 완료 기준 / 검증 방법

- **검증 방식**: 사람이 정답을 달아놓은 테스트셋 대비 정확도 측정
  - 목표: 프로그램이 자동으로(플래그 없이) 수정한 항목 중 오류 0건
- **테스트셋 출처**: 직접 수집/제작 (실제 유튜브 영상·STT 결과물을 모아 정답
  라벨링)

## 8. 의존성 / 선행 작업

- [x] 국립국어원 Open API 키 신청 (표준국어대사전, 우리말샘)
- [x] 한국어 어문 규범 Open API 키 신청 (kornorms, 외래어·로마자 표기 용례)
- [x] 온용어(K-term) Open API 키 신청 (다듬은 말/표준 전문용어 조회)
- [x] 한국어기초사전 Open API 키 신청 (초급자 대상 사전)
- [x] 지역어 종합 정보 Open API 키 신청 (방언↔표준어, 함수만 추가, 파이프라인
      미연결)
- [x] 온라인가나다 판례 축적 — 전체 크롤링 대신, 실제로 쓰이는 사례를 배치로
      조사해 쌓는 방식으로 구현 (`docs/log-archive/2026-h2.md` §17 참고)
- [~] 테스트셋 수집 및 정답 라벨링 — **41건**: `examples/eval/heldout.jsonl`에
      라벨 41건(id/category/input/gold/basis/verify)과 실행기
      `examples/eval/run_eval.py`. 라벨은 `tools/verify_eval_labels.py`로 사전
      검증해서 붙인다. 아직 작은 편이라는 한계는 그대로다 — 실사용 감수에서 새
      사례가 나오면 계속 보탤 것.

## 9. 향후 확장 로드맵 (v1 이후)

지금 만드는 것은 로컬 CLI 도구이지만, 나중에 서비스 형태(웹/백엔드)로 확장할
가능성을 고려해 아래 항목을 로드맵으로 남겨둔다. v1 설계에는 §4의 "아키텍처
원칙"(교정 엔진과 인터페이스 분리)만 반영하고, 아래 기능 자체는 만들지 않는다.

- **로그인/계정**: 사용자별 처리 이력 관리를 위해 필요. 인증 방식(자체 구현 vs
  OAuth 등)은 미정. 아직 미구현 — §11의 저장은 로그인 없이 "id를 아는 사람은
  누구나 그 결과를 볼 수 있다"는 수준(짧은 링크 공유 정도의 보안)이다. 여러
  사용자를 구분해서 관리하려면 이 항목이 선행되어야 함.
- **저장 — 구현 완료(§11 참고)**: 업로드한 문서의 교정 결과와 플래그 리포트를
  Supabase(Postgres)에 저장. 저장 기간·개인정보(문서 내용) 처리 정책은 아직
  정하지 않음(무기한 보관 중) — 실 서비스로 나갈 경우 반드시 결정 필요.
- **결제**: 유료 플랜/사용량 과금 여부. 어떤 기능을 유료화할지(예: 처리량,
  맞춤법 API 호출 횟수)는 미정
- **도메인별 전문용어 사전**: 의학 용어 사전(KMLE) 등 특정 분야(의학·법률 등)
  전문용어 교정. 국립국어원 공식 오픈API가 아닌 민간 사이트라 이용약관 확인이
  선행되어야 하고, 일반 한국어 텍스트라는 v1 범위를 벗어나므로 v2 이후로 미룸
- 이 로드맵이 구체화되면 별도 PRD로 분리해서 다룬다 (백엔드 아키텍처, 인증,
  결제 연동은 이 문서의 범위를 벗어남)

## 10. TBD (추후 결정 필요)

- 개발 일정 / 목표 기간
- 배포 방식 (pip 패키지 등)
- 리포트 파일 정확한 포맷 스펙 (json/csv 등)
- 처리 속도 등 성능 목표
- 세부 모듈 아키텍처, 에러 핸들링 정책

## 11. 웹 API / 저장 (구현 완료)

수업 과제("내 서비스에 백엔드 붙이기")로 §9의 "저장" 항목을 실제로 구현했다. 새
교정 로직은 추가하지 않고, §4의 아키텍처 원칙대로 기존 엔진을 그대로 호출만
한다.

- **구성**: `subtitle_corrector/api.py`(FastAPI) — `POST /api/correct`(`.srt`
  업로드 → `parsers.parse_srt` → `engine.correct_entries` → 결과 저장) /
  `GET /api/reports/{id}`(저장된 결과 재조회). `static/index.html`이 업로드
  화면 겸 결과 화면(`?id=`로 새로고침·다른 기기에서도 결과 유지).
- **저장소**: Supabase(Postgres), `subtitle_corrector/store.py`가
  REST(PostgREST)를 `requests`로 직접 호출. 테이블 스키마는
  `supabase_schema.sql`.
- **보안 설계 — 강의 자료의 "브라우저가 Supabase에 직접 접근" 패턴이 아니다**:
  브라우저는 이 FastAPI 서버에만 요청하고, Supabase에는 절대 직접 접근하지
  않는다. 따라서 `SUPABASE_SERVICE_KEY`(관리자 키, RLS 무시)를 서버 쪽
  환경변수로만 갖고 있어도 안전하며, `reports` 테이블은 RLS를 켜두고 정책을
  하나도 만들지 않아 anon(공개) 키로는 어떤 요청도 거부된다 — 서버가 유일한
  접근 경로.
- **비밀값**: `STDICT_API_KEY`/`OPENDICT_API_KEY`/`KORNORMS_API_KEY` 등 +
  `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`, 전부 `.env`(로컬)로만 존재.
  `.env.example` 참고.
- **클라우드 배포는 하지 않는다(결정, 종결)**: kiwipiepy 모델 메모리 사용량
  (약 310MB)이 무료 티어 512MB 한도를 거의 다 써버려 배포 시 502 에러가 났다.
  설정 문제가 아니라 자원 한계이며 지연 로딩으로도 해결되지 않는다(기동만
  통과, 첫 교정에서 같은 한도). GitHub에 커밋·푸시만 하고 실행은 로컬에서
  한다. 배포 시도 경위는 `docs/RETROSPECTIVE.md`, 관련 결정은
  `docs/BACKLOG.md` 참고.
- **테스트**: `examples/sample.srt`로 FastAPI 라우트(200/404, 파일 형식 검증)와
  엔진 연동을 확인함(`store.save_report`/`get_report`를 스텁으로 교체해 실제
  Supabase 계정 없이 검증).
- 웹 API/저장 관련 실사용 버그, kiwi 미등록 단어 오탐지, 보안/SQL 종합 검토,
  저장 실패 격리 등 상세 경위는 `docs/log-archive/2026-h2.md` 참고.

## 12. 사투리 교정 (구현 완료)

**핵심 원칙**: 대본에 쓰인 사투리는 거의 항상 작가의 **의도**다. 따라서 기본적
으로 사투리를 표준어로 바꾸지 않으며, 오류로 플래그하지도 않는다. "의도된
사투리"와 "오타"를 확실히 구분하는 것은 판별 불가능한 경계 사례이므로, 확률적
추정(heuristic)으로 자동 수정하지 않고 사람 검토에 맡긴다(§NNP 안전 정책과
동일한 원칙).

- **화자별 사투리 설정**: 사용자가 각 화자에게 사투리 지역을 지정할 수 있다
  (경상도·제주도·전라도·충청도). SDH 브래킷([이름])에서 화자를 추출한다.
- **지정된 화자의 처리 모드**(`dialect_modes`, 화자별):
  - **protect** — 지정된 화자의 **기본값**. 대사를 완전히 그대로 둔다.
    표준화 자동 교정(외래어·조사/합성어/보조용언 띄어쓰기·항상 틀린 표현·비표준
    어·차별어)도, 인명/고유명사 검토 플래그도, 맞춤법·순화어·띄어쓰기 검사도
    **전부 건너뛴다**. 사투리 플래그도 남기지 않는다. (감수하는 트레이드오프:
    보호된 대사 안의 진짜 오타도 그대로 둔다 — 사투리와 오타를 신뢰성 있게
    구분할 수 없기 때문이며, 이런 경계 사례에 확률적 자동 수정을 만들지 않는
    것이 프로젝트 정책이다.)
  - **assist** — 작가가 사투리 화자를 원하지만 사투리에 익숙하지 않을 때.
    텍스트를 자동으로 바꾸지 않고 표준화 파이프라인도 돌리지 않는다(표준화는
    의도와 정반대). 대신 표준어→사투리 형태를 제안하는 플래그(suggested_fix)만
    남긴다. `convert_dialect(text, region, "to_dialect")`가 바꿀 게 없고
    `search_dialect()`도 비면 플래그를 남기지 않는다. 채택 여부는 사람이
    리포트에서 결정한다.
  - **to_standard** — 드문 opt-in.
    `convert_dialect(text, region, "to_standard")`가 내놓는 결과를 **제안
    플래그로만** 남긴다(자동 적용이 아니다 — 근거 없는 표지 사전이 '노래'를
    '노라고'로 깨뜨린 사고 이후 변경). 이 화자는 표준 출력을 원하므로 원문에
    일반 표준화 파이프라인은 그대로 적용한다.
  - **하위 호환**: 옛 모드명 `flag_only`→`protect`, `to_dialect`→`assist`로
    매핑한다(옛 호출부 호환).
- **문서 전체 사투리 설정**: 화자별 지정이 없는 줄에 적용되는 문서 단위
  (지역, 모드). 소설처럼 글 전체가 한 사투리인 문서를 다루기 위한 것이다(예:
  박경리 '토지'를 현대 표준어로). 일반 글에는 화자 표기 자체가 없어서, 이
  설정이 없으면 사투리 기능이 아예 걸리지 않는다. 화자별 지정이 있으면 그쪽이
  우선한다(`resolve_dialect_mode`의 fallback). 모드 기본값은 여전히 protect —
  글 전체를 말없이 표준어로 바꿔 버리면 안 되기 때문이다. 노출:
  `correct_entries(dialect_region=, dialect_mode=)`, `/api/correct`의
  `dialect_region`·`dialect_mode` Form(지원하지 않는 지역명은 무시),
  `main.py --dialect-region/--dialect-mode`, 웹 UI의 "문서 전체" 드롭다운.
  - **한계**: 변환 품질은 `convert_dialect()`의 정적 매핑에 달려 있고 지역어
    API는 서버 장애로 멈춰 있다(`docs/BACKLOG.md`). 실측: "이거 아이가 마이시
    좋다" → "이거 그래 많이 좋다"는 되지만 "밥은 뭇나 안 뭇나"는 그대로다.
    **문학 사투리 전면 표준어화는 아직 부분적으로만 된다.**
- **미지정 화자**: 기존 동작 유지. 표준화 파이프라인을 돌리고, 이름이 있으면
  자동으로 사투리 패턴을 감지해(비율 >= 0.15) 플래그만 남긴다(사투리 지정을
  권유). 자동교정은 하지 않는다.
- **구현**: `dictionary.py`(DIALECT_MARKERS, detect_dialect_ratio,
  convert_dialect, search_dialect), `engine/options.py`(normalize_dialect_mode,
  resolve_dialect_mode)·`engine/dialect.py`(check_dialect)·`engine/pipeline.py`
  (correct_entries에서 모드 우선 게이팅), `api.py`(/api/speakers,
  /api/dialect-regions, dialect_map·dialect_modes 파라미터, 모드
  검증/정규화), `static/index.html`(화자별 지역·모드 드롭다운). 테스트:
  `tests/test_dialect_modes.py`.
- **알려진 한계**: 지역어 종합 정보 API(dialect.korean.go.kr)가 현재 JSON이
  아닌 응답을 돌려주어 `search_dialect()`는 사실상 빈 결과를 반환한다. assist
  제안은 정적 `convert_dialect` 매핑에만 의존한다.

## 13. 띄어쓰기 기준 선택 — 원칙 / 허용 (구현 완료)

**배경**: 한글 맞춤법 제47항은 보조 용언을 "띄어 씀을 원칙으로 하되, 붙여 씀도
허용"한다. 둘 다 맞는 표기이므로 어느 쪽을 쓸지는 규범이 아니라 **작품의
선택**이다. 다만 한 작품 안에서 두 기준이 섞이면 그 자체가 교정 대상이므로,
사용자가 기준을 고르면 문서 전체가 그 한쪽으로 통일되어야 한다.

- **기준값**(`spacing_mode`, 문서 단위 — 줄마다 다르게 줄 수 없다):
  - **principle** — 기본값. 붙여 쓴 보조 용언을 띄어 쓴다(`할만하다` →
    `할 만하다`).
  - **allowance** — 띄어 쓴 보조 용언을 붙여 쓴다(`해 보자` → `해보자`,
    `올 듯하다` → `올듯하다`).
  - **선택지는 이 둘뿐이다.** 한때 있었던 `keep`(원문 유지) 옵션은
    폐지됐다(`docs/log-archive/2026-h2.md` §74) — 기준을 고르지 않는다는 것은
    혼용을 그대로 두겠다는 뜻인데, 혼용은 규범이 인정하는 선택이 아니라 교정
    대상이기 때문이다. 옛 요청·저장된 설정의 `keep`은 `principle`로 흡수한다.
    말줄임표·따옴표의 '원문 유지'는 성격이 달라 그대로 둔다.
  - 모르는 값·빈 값은 `principle`로 정규화한다(`normalize_spacing_mode`).
    규범 기본값이 아닌 쪽으로 문서 전체가 조용히 바뀌면 안 되기 때문이다.
- **붙임이 불가능한 구간**은 allowance에서도 띄어 쓴다. 이건 기준 혼용이
  아니라 제47항 자체의 예외이므로, 자동 교정 로그에 `[붙임 불가]` 사유를
  남긴다.
  - 본용언이 3음절 이상 사전 등재 합성어(`덤벼들어 보아라`) — 제47항 단서상
    항상 띄어 쓴다.
  - 의존명사+하다/싶다 조합이 사전에 없는 경우(`만싶다`) — 붙임 근거가 없어
    그대로 둔다.
- **자동 통일의 적용 범위는 제47항 보조 용언뿐이다.** 제49항(고유명사)·
  제50항(전문 용어)은 자동 통일 대신 혼용 감지로 처리한다 — §14 참고.
  제46항(단음절 단어 연속)은 미착수다.
- **구현**: `engine/options.py`(`SPACING_MODES`, `normalize_spacing_mode`)·
  `engine/spacing.py`(`_aux_verb_spacing(text, mode)` — 기존
  `correct_aux_verb_spacing`은 원칙 전용 얇은 래퍼로 남김,
  `correct_entries(spacing_mode=...)`), `api.py`(`/api/correct`의
  `spacing_mode` Form), `main.py`(`--spacing principle|allowance`),
  `static/index.html`(띄어쓰기 기준 라디오). 테스트:
  `tests/test_engine.py::TestAuxVerbSpacingAllowanceMode`.
- **번역본 PRD 미반영**: `PRD.en.md` 등 6개 언어 사본에는 이 절이 없다.

## 14. 고유명사·전문 용어 띄어쓰기 혼용 감지 (구현 완료)

**규칙**:

| 대상 | 원칙 | 허용 |
|---|---|---|
| 고유명사(제49항) | 다 띄어 씀 — `마포 경찰서 강력 팀` | **의미 단위별** 붙임 — `마포경찰서 강력팀` (부분 붙임 가능) |
| 전문 용어(제50항) | 다 띄어 씀 — `만성 골수성 백혈병` | **전부** 붙임 — `만성골수성백혈병` (부분만 붙이는 것 불가) |
| 거리 이름 | `대로/로/가`는 이름에 **붙여 씀**(`세종대로`, `충무로`, `종로2가`) / `길·거리`는 띄어 씀(`개나리 길`) | `길·거리`도 고유명사로 보아 전부 붙임 허용(`개나리길`) |

- **처리 방식**: §13에서 고른 기준대로 **양방향 자동 통일**한다
  (`unify_term_spacing(mode)`) — 혼용이라는 말 자체가 두 표기가 모두 문서에
  있다는 뜻이라 이 자리에서는 붙임 경계를 추측할 일이 없다. 목표 표기를 새로
  만들어야 하는 자리만 손대지 않는다(허용인데 `만성골수성 백혈병`처럼 부분
  붙임밖에 없으면 제50항 위반이라 그대로 둔다, 도로명은 붙임이 원칙이라
  제외). 혼용이 아닌 자리도 허용 기준에서는 붙인다 — 표준국어대사전이
  캐럿으로 적는 전문 용어 명사구(`무게^중심`·`예방^접종`)가 곧 제50항이 말하는
  전문 용어라는 사전의 확인이므로 `join_term_phrases()`가 그 구간만 붙인다.
  - (최초 설계는 "자동 통일이 아니라 혼용 감지 플래그"였다 — 붙이려면
    어디까지가 한 용어인지 알아야 하는데 그 경계가 사전에 없고 문맥 추측이
    필요해 §5의 원칙에 걸린다는 이유였다. 위 방식으로 뒤집힌 경위는
    `docs/log-archive/2026-h2.md` §73 참고.)
- **판정 후보 구간**: `_TERM_RUN_TAGS`(= `_TERM_COMPOUND_TAGS` + 어근 XR +
  명사 파생 접미사 XSN) 토큰이 공백만 사이에 두고 이어지는 구간과 그 부분
  구간. 공백 제외 4~30자, 최대 6토큰. 부분 구간까지 내는 이유는 `만성 골수성
  백혈병 진단`과 `만성골수성백혈병 치료`처럼 뒤에 붙는 말이 다르면 최대
  구간만으로는 짝지어지지 않기 때문이다. 같은 혼용이 여러 길이로 걸리면 가장
  긴 것만 남긴다.
- **의존명사(NNB) 제외**: `말한 대로`/`말한대로`는 제42항이 항상 띄어 쓰라고
  정한 별개 규칙이라, 여기 섞으면 정당한 오류를 통일 문제로 오인하게 된다.
- **통일 후보(`suggested_fix`) 우선순위**: 거리 이름 규칙을 어기지 않는 표기 →
  문서에서 더 자주 쓴 표기 → 동률이면 원칙(공백이 많은 형태) → 사전순.
- **구현**: `engine/consistency.py`(`_term_runs`, `_violates_street_name_rule`,
  `check_term_spacing_consistency` — 줄 단위 파이프라인이 끝난 뒤 문서 전체를
  한 번 훑는다), 사투리 protect 화자의 대사는 제외. 테스트:
  `tests/test_engine.py::TestTermSpacingConsistency`.

## 16. 자막 편집 표지 설정 (구현 완료)

자막 원고에는 어문 규범의 대상이 아닌 **기술적 표지**가 섞인다. 업계 공통
규칙이 없어 작업자·편집기마다 값이 달라서, 값을 고정하지 않고 **그때그때
설정으로 받아** 교정에서 제외한다(사용자 지정).

| 표지 | 예 | 처리 |
|---|---|---|
| 화면자막 표기 | `"…"`, `@…` | 짝이 있는 문자면 감싸인 **구간만**, 짝이 없는 문자면 **그 줄 전체**를 제외 |
| 줄바꿈 표기 | `\|` | 교정할 때만 **실제 줄바꿈으로 취급**하고, 결과에는 표지를 그대로 되돌림 |
| 자막 위치 표기 | `{\an8}` | 제어 코드라 통째로 보호 |

- **짝 판단은 형태로 자동**(`_MARKER_PAIRS`: 따옴표·괄호류). 사용자는 문자만
  적고 방식을 고를 필요가 없다. 여는 표지를 닫지 않은 경우는 줄 끝까지
  보호한다 — 그 뒤는 화면자막일 가능성이 높고, 교정하지 않는 쪽이 덜 위험하다.
- **제외 강도는 완전 제외**: 보호 구간에는 자동 교정도 확인 플래그도 없다
  (사투리 protect 모드와 같은 방식). 화면에 뜬 글자를 그대로 옮긴 것이라
  규범 적용 대상이 아니기 때문이다.
- **줄바꿈 표지를 실제 줄바꿈으로 보는 이유**: 자막 모드에는 각 줄 끝 마침표를
  생략하는 규칙(`correct_subtitle_final_period`, `engine/subtitle_rules.py`)이
  있는데, 그 판정이 화면에 보이는 줄과 같은 기준으로 적용되려면 이 표지도
  실제 줄바꿈으로 취급해야 한다. 표지만 보호하면
  `안녕하세요.|반갑습니다.`의 앞 마침표가 줄 끝으로 안 잡힌다. (이 문단이
  한때 가리키던 "§15 구두점 규칙"은 PRD에 독립 절로 존재한 적이 없다 — 경위는
  `docs/log-archive/2026-h2.md` 참고.)
- **자막 모드에서만 적용**한다. 일반 글에는 이런 편집 표지가 없다.
- **구조**: `correct_entries()` 안에 있던 줄 단위 파이프라인을
  `_correct_line()`으로 순수 이동하고, 표지 처리는 그 위의
  `_correct_line_with_markers()`가 맡는다. 플래그는 구간이 아니라 **줄 전체
  기준으로 되돌린다** — 조각으로 남으면 `apply-report`가 그 조각으로 줄
  전체를 덮어써 나머지 대사를 지운다.
- **노출**: `correct_entries(markers=)`, `/api/correct`의
  `screen_text_marker`·`line_break_marker`·`position_marker` Form,
  `main.py --screen-text-marker/--line-break-marker/--position-marker`, 웹 UI
  "자막 편집 표지" 입력 3칸. 테스트: `tests/test_subtitle_markers.py`.

## 17. 자막 읽기 속도(CPS)·한 줄 글자 수 검사 — 제거됨

**사용자 결정으로 삭제했다.** 읽기 속도와 줄당 글자 수는 자막 도구의 기능이지
교정기의 기능이 아니다 — 이 도구는 어문 규범과 자막 표기 관례를 다루고,
시간·배치 제약은 그쪽이 다룬다. 이후 자막 및 TC 생성기(별도 프로젝트)가 이
검사를 흡수했다 — 이 자리에서 뺀 판단이 옳았던 셈이다. 삭제 전 설계와 검증
기록은 `docs/log-archive/2026-h2.md`의 "삭제된 기능 설계 기록" 부록 참고.

## 17-1. PDF 입력은 텍스트 레이어만 읽는다 (OCR 제거)

PDF를 입력으로 받지만 **OCR은 하지 않는다.** 스캔본처럼 텍스트 레이어가 없는
PDF는 글자를 뽑지 못하고, 그 사실을 그대로 알린다.

**왜 뺐나.** OCR이 만든 글자는 원문이 아니라 **추정**이다. 이 도구의 최우선
원칙은 "권위 있는 근거 기반, 애매하면 사람에게 확인"인데, OCR 오독을 원문으로
삼으면 그 위에 올린 교정이 전부 근거를 잃는다. `l`과 `1`, `ㅇ`과 `ㅁ`을 잘못
읽은 자리에 맞춤법 규칙을 걸면 **원문에 없던 오류를 만들어 놓고 고치는**
꼴이 된다.

## 18. 자막 표시 띄어쓰기 규칙 (구현 완료)

자막 위치 표기(`{n8}`)·화자명 표기(`[민수]`)·어조/지문 표기(`(웃으며)`)는
대사가 아니라 **편집·전달용 표시**다. 사용자 지정 규칙은 둘뿐이고, 둘 다
정답이 하나라 자동 교정한다.

1. **표시끼리는 붙여 쓴다.** 연달아 올 때 사이에 공백이 있으면 오류다 —
   `{n8} [민수]` → `{n8}[민수]`.
2. **표시와 말자막 사이는 정확히 한 칸.** 붙어 있거나 여러 칸이면 오류다 —
   `[민수]안녕` → `[민수] 안녕`.

- 표시만 있고 뒤에 대사가 없는 줄(효과음 등)은 건드리지 않는다.
- **설정에서 받은 부호만 표시로 본다.** 대괄호만 쓰는 원고에서 괄호까지
  표시로 취급하면 정당한 표기를 망친다.
- 위치 표기는 교정 파이프라인에서 보호 조각으로 먼저 떼어지므로, 인접 규칙은
  조각을 **합친 뒤에** 한 번 더 적용한다. 그러지 않으면 조각 경계를 사이에 둔
  공백(`{n8} [민수]`)을 볼 수 없다.
- 구현: `engine/markers.py`의 `_marker_unit_pattern`·`engine/subtitle_rules.py`
  의 `correct_subtitle_bracket_spacing`. 테스트:
  `tests/test_subtitle_markers.py::TestMarkerAdjacency`.

## 19. 언어 모델 패스 (구현 완료 · 선택 기능, 기본 꺼짐)

**규칙 교정이 모두 끝난 뒤에** 남은 텍스트를 모델에게 보여 주고, 문맥 판단이
필요한 자리만 확인 항목으로 더 받는다. 구현은 `engine/llm_pass.py`, 시험은
`tests/test_llm_pass.py`.

지켜야 할 선(설계의 핵심):

- **본문은 바뀌지 않는다.** 모델에는 사전 같은 확정 근거가 없다. 제안만 하고,
  사람이 고른 것만 반영된다.
- 모델이 낸 제안도 규칙 교정과 **똑같은 `edit_guard` 관문**을 통과해야 목록에
  오른다. 근거를 밝히지 않은 변경·줄바꿈 개수를 바꾸는 변경은 버려진다.
- **줄 세우는 순서가 곧 원칙이다.** 규칙·사전이 먼저 판정하고 모델은 그 뒤에
  선다. 반대로 놓으면 확률적 판단이 확정 근거를 덮는다.
- 설정은 `LLM_BASE_URL`·`LLM_MODEL`·`LLM_API_KEY`. OpenAI 호환 주소라 로컬
  (Ollama·llama.cpp·vLLM)과 상용 API를 같은 설정으로 쓴다. **타임코드는
  보내지 않는다.**
- **출력 형식은 지시문이 아니라 스키마로 받는다**(2026-09-01, §78). "JSON만
  내라"는 지시문은 부탁이라 확률적으로만 지켜진다 — `response_format`으로 서버가
  강제하게 하면 형식이 어긋난 응답 자체가 나올 수 없다. 그 자리를 모르는 서버에는
  한 번만 스키마 없이 다시 물어보고, 이후 구간부터는 시도하지 않는다.
- **응답에서 목록을 못 꺼내면 반드시 알린다**(§78). 검토되지 않은 구간과 "고칠
  것이 없다"를 같은 빈 결과로 뭉개면 사용자는 뒤쪽으로 읽는다 — `search_dialect()`가
  장애와 매칭 없음을 구분 못 해 겪은 것과 같은 자리다.
- 값을 넣지 않으면 도구는 지금까지와 똑같이 동작한다.

**§20의 결론과 모순이 아니다.** 판정에는 AI를 얹지 않고, 제안에만 쓴다.

## 20. MQ2 PoC — 띄어쓰기 교정에 AI를 얹지 않는다

물음: **"한국어 띄어쓰기 교정에 AI를 넣으면 나아지는가."** 이 교정기는
규정·사전·판례를 이미 갖고 있고, 문제는 그 안에서 문맥과 상관없이 아무거나
골라 띄거나 붙이는 것이었다. 그 자리에 문장 임베딩 검색과 로컬 LLM을 얹어
실측했다.

**결론: 얹지 않는다.** 둘 다 신경망을 쓰지 않는 방법에 졌다. 이미 맞게 쓴
문장을 망가뜨린 것은 임베딩뿐이었다. 정답과 근거가 문서로 확정된 영역에서는
확률적 생성이 강점이 아니라 약점이고, 무엇보다 **확률 모델은 모를 때 기권할
줄 모른다.** 이 도구의 최상위 원칙(모호하면 사람에게 확인)과 정면으로
어긋난다.

이어진 실측에서 나온 것: **뜻풀이를 읽으면 과잉교정이 0이 된다.** 사전
표제어가 있느냐만 보지 않고 그 뜻이 문맥에 맞느냐까지 보면, 우연한 사전
충돌(원리 3)이 사라진다.

자료: `poc/mq2-spacing-retrieval/` — 실험 2회, 평가셋 76건, 비교 대상 7종,
실사용 오교정 사례집 50건, 재현 절차, 실패 사례, 한계와 다음 단계.
