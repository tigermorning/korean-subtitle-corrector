# 저장소 안에서는 항상 LF로 저장한다. WSL(LF)과 Windows(CRLF)를 오가며 작업하면
# 내용이 그대로인 파일이 통짜 변경으로 떠서 실제 변경이 묻힌다.
* text=auto eol=lf

# 바이너리는 손대지 않는다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.docx binary
*.pdf binary
