# 밀린 작업 목록

`PRD.md`에서 분리된 문서(2026-07-27). PRD.md는 잘 안 바뀌는 핵심 스펙만 담는 문서인데 이 목록은 항목이 끝나고 새로 생길 때마다 계속 바뀌므로 따로 뒀다.

**세션을 시작하면 다른 작업보다 먼저 이 파일을 읽고 사용자에게 보고할 것.**

**작성 이유**: 밀린 작업이 `PRD.md`·`docs/IMPLEMENTATION_LOG.md`·`docs/GRAMMAR_PRECEDENTS_TABLE.md`·§8 체크리스트에 흩어져 있어서 사용자가 존재 자체를 모르고 있었다. 앞으로 새 세션은 **다른 작업을 시작하기 전에 이 목록을 먼저 사용자에게 보고**하고, 위에서부터 처리한다. 항목을 끝내면 이 목록에서 지우고, 새로 미룬 일이 생기면 반드시 여기에 추가한다 — 다시 흩어지면 이 목록의 의미가 없다.

### 검증 상태 (2026-07-27 코드 기준 전수 확인)

아래 목록은 문서 기록이 아니라 **코드를 직접 확인해서** 상태를 매긴 것이다. 확인 과정에서 문서가 낡아 있던 3건을 찾아 해당 줄을 그 자리에서 정정했다(`docs/IMPLEMENTATION_LOG.md:100`, 같은 파일 `:177`, `PRD.md` §8 체크리스트). **문서가 "미구현"이라고 말해도 코드를 먼저 확인할 것** — 이 확인만으로 완료된 작업 1건과 부분 완료 2건이 드러났다.

**열려 있는 항목: 10건** (2026-08-02 기준. 8·20번은 사용자 결정으로 종결, 15·18·19번은 같은 날 해결, 16이 열려 있다.)

| 성격 | 건수 | 항목 |
|---|---|---|
| 코드 작업 | 7 | 1(선행 조건 미충족으로 대기), 2, 3, 4, 6, 7, 16 |
| 부분 완료 — 보탤 일 | 1 | 9 (테스트셋 확대) |
| 리팩터링 | 1 | 14 (`engine.py` 분할) |
| 선행 결정 대기(LLM 연동) | 1 | 17 (교정 강도 선택) |

**지금 바로 착수 가능한 코드 작업은 5건**(2·3·4·6·7)이다. 1번은 `PRECEDENTS`가 0건이라 판례를 채우는 선행 작업이 끝나야 시작할 수 있다.

2026-07-27에 처리·정리한 것: 5번(지역어 API 장애 확진 — 서버측이라 우리 작업 없음), 10번(회고 "아쉬운 점" 작성), 11번(로그 아카이브 2회차), 12번(§31·§32 참조 수정), 13번(낡은 기록 3곳 정정). 이미 완료라 목록에서 제거한 것: 구 6번 지역어 API 파이프라인 연결(`engine.py:52` import, `engine.py:1570` 호출 확인).

### 착수 가능 (근거 확보됨, 코드만 남음)

1. **유사 판례 검색 실측 감수** — 위 "바로 다음 할 일 ①" 참고. 사용자가 최우선으로 지정(2026-07-27). **확인: `PRECEDENTS`는 0건**이라 착수 불가 상태이므로 판례 채우기가 선행이다.
2. **§27 "명사+드리다" 오탐지 조사·구현** — **확인: `subtitle_corrector/`·`tests/` 전체에 "드리다" 언급 0건.** 손댄 적 없음. `docs/log-archive/2026-h2.md` §27 전체를 먼저 읽을 것.
3. **제42항 의존명사 6개(들/뿐/만/지/차/판) 코드 구현** — **확인: 코드 0건, 검증표는 완료**(`docs/GRAMMAR_PRECEDENTS_TABLE.md`에 6개 모두 판정 규칙 + `qna_seq` 근거 + 2026-07-20 확인일까지 기재). **단, 값싼 작업이 아니다** — 검증표 스스로 '차'는 접미사·의존명사 두 종류가 겹치는 3중 기능이라 "표면만으로 구분 불가, 100% 커버 불가능한 경우가 남을 수 있음", '들'은 "나열 구조를 감지해야 해 구현 난이도가 상대적으로 높음"이라고 적어 두었다. 6개 모두 단일 True/False 판정이 아니라 **문맥 규칙**으로 구현해야 한다. 착수 시 `qna_seq` 인용을 다시 확인할 것(2026-07-20 배치에서 404가 1건 있었다).
4. **제44항 수 표기("몇만/몇백만") 자동교정 승격** — **확인: "몇만"·"몇백만"에 대한 코드·테스트 0건.** 이전 기록의 "현재 플래그만"은 부정확하다. 플래그도 없고 아무 처리도 없다. 제44항 관련 기존 테스트는 `tests/test_engine.py:286`의 "20만 명"을 쪼개지 않는지 확인하는 것뿐이라 이 항목과는 별개다.

15. **조사 '에서'를 '에'+'서다'로 갈라놓는 과교정 — 해결(2026-08-02)**. 원인은 kiwi 1순위 분석이 '에'(조사)+'서'(서다)였던 것. 대안 후보에 '에서'(조사 하나)가 있으면 자동으로 갈라놓지 않는다(`_merged_particle_reading_exists`). 아래는 발견 당시 기록이다. — 2026-08-02 `docs/IMPLEMENTATION_LOG.md` §37 작업 중 부수적으로 발견. `개나리길 입구에서 봐` -> `개나리길 입구에 서 봐`로 자동 교정된다(자동 교정 로그에 남음, 플래그 아님). `에서`는 한 덩어리 부사격 조사라 갈라선 안 된다. 어느 함수가 만드는지부터 확인할 것(`correct_particle_spacing`/`_mechanical_respace` 부근으로 추정). 자동 교정이라 사람이 못 보고 지나칠 수 있어 우선순위가 낮지 않다.
16. **거리 이름 `대로/로/가` 띄어 쓴 표기 자체를 플래그할지 결정** — 2026-08-02 `docs/IMPLEMENTATION_LOG.md` §37에서는 혼용이 있을 때 통일 후보를 고르는 데만 이 규칙을 썼다. `세종 대로`가 문서에 한 번만 나오면 지금은 아무 말도 하지 않는다. 규정상 붙임이 원칙이라 플래그할 근거는 있지만, `대로`가 일반명사로 쓰인 경우(`왕복 8차선 대로`)와 구분할 방법을 먼저 정해야 한다.

18. **사전 미등재 고유명사 분리 오탐(`한짓골` -> `한 짓골`) — 해결(2026-08-02)**. 아래는 해결 전 기록이다. `한`이 관형사(MM)로 태깅돼 `_TERM_COMPOUND_TAGS` 보호에서 빠진다. 사전에 없는 지명이라 사전으로는 방어할 수 없고, 현재 수단(고유명사 입력칸, 3회 이상 반복 자동 인식)은 사극처럼 한 번만 나오는 지명에 안 걸린다. **검토안**: 분리 제안이 만드는 조각이 전부 사전에 없으면 제안하지 않는다(`짓골` 미등재 -> 취소). 정당한 분리에도 사전에 없는 조각이 생기는 경우가 있어(활용형·조사 결합) 회귀 위험 측정이 선행이다.

19. **느낌표 뒤 띄어쓰기 제안 — 원인 규명·해결(2026-08-02)**. 느낌표가 아니라 감탄사 오분석이었다(`에라이!` -> `에라,이!`). 아래는 재현 실패 당시 기록이다. — 사용자가 "구두점은 늘 글자 바로 뒤에 온다"며 오탐을 보고했으나 `야! 이리 와`·`정말 대단해!`에서 재현되지 않았다. 원문(문장부호·앞뒤 문맥 포함)을 받으면 다시 볼 것. 같은 날 `말라 그래`도 마침표가 있어야만 재현됐다 — **오탐 재현은 사용자가 본 원문 그대로 넣어야 한다.**

20. **욕설 마스킹 — 자동화하지 않음(2026-08-02, 사용자 결정)** — `이 새끼가` -> `이 새*가`처럼 어절에 맞춰 별표로 가리는 관례가 있으나, **가리는 범위가 이용 연령 등급에 따라 달라진다**(`씨발` -> `**`, `개새끼` -> `개**`가 흔하지만 등급에 따라 다름). 등급은 텍스트에서 알 수 없으므로 자동으로 정할 근거가 없다. 사용자 판단에 맡기고 이 도구는 손대지 않는다. 되살린다면 "무엇을 어디까지 가릴지"를 설정으로 받는 형태여야 하며, 사용자가 먼저 요청하기 전에는 제안하지 말 것.

### 외부 요인 대기 / 규모 큼

5. **`search_dialect()` — 확인 완료(2026-07-27), 우리 쪽 작업 없음** — 서버측 500 장애가 지속된다. 원 요청(`requests.get`)으로 `정구지`·`머스마`·`무싯날`·`감자` 4개 질의를 보냈고 **전부 HTTP 500 + HTML 오류 페이지**를 받았다. 기존 문서 기록("non-JSON 반환으로 사실상 중단")이 맞았다 — 앞서 이 항목에 "상태가 문서와 다르다"고 적은 것은 오판이었다. `search_dialect()`가 실패를 삼키고 `[]`를 돌려주는 것을 "정상 응답, 매칭 없음"으로 잘못 읽었기 때문이다. **상태 확인은 반드시 원 요청으로 할 것.** 서버가 복구되기 전에는 할 일이 없다.
6. **표준국어대사전 사전 내용 API(`view.do`)** — **확인: 코드 0건.** 용례·`norm_info`(한글 맞춤법 근거) 확인용. 근거 품질을 올릴 여지.
7. **온라인가나다 연동 2단계** — **확인: 코드 0건**(`docs/GRAMMAR_PRECEDENTS_TABLE.md` 문서에만 `qna_seq` 언급). 남은 작업 중 최대 규모. 깔끔한 API가 없어 검색/크롤링 방법 조사부터 필요(§8).
8. **클라우드 배포 — 종결(2026-08-02, 사용자 결정: 하지 않는다)**
   - **결정**: GitHub에 커밋·푸시만 제대로 하고, 실행은 로컬(`uvicorn` 또는 CLI)에서 한다. 유료 전환도, 다른 호스팅(HuggingFace Spaces 등) 이전도 하지 않는다.
   - **근거(2026-08-02 재확인)**: `https://korean-subtitle-corrector.onrender.com/`은 `GET /`가 200(정적 HTML이라 메모리를 안 씀)이지만 `POST /api/correct`는 **502**다. 2026-07-22 진단과 동일 — kiwipiepy 모델 약 310MB에 무료 티어 512MB라 교정 요청 시 한도를 넘는다. 설정 문제가 아니라 자원 한계라 코드로 우회할 수 없다(지연 로딩은 기동만 통과시키고 첫 교정에서 같은 한도에 부딪힌다). kiwi를 빼는 선택지도 없다 — 이 프로젝트의 모든 판정이 kiwi 형태소 태그 위에 서 있다.
   - **남아 있는 사실**: 그 URL은 2026-07-26~08-02 사이 커밋으로 떠 있어 최신 UI(띄어쓰기 기준 선택, 혼용 감지, 접기 패널)가 반영돼 있지 않다. 살아 있는 것처럼 보이지만 교정은 되지 않는 상태다. **이 URL을 동작하는 데모로 안내하지 말 것.** 배포를 되살리기로 마음이 바뀌면 이 항목부터 다시 열 것.

17. **교정 강도 선택(보수적/일반/리라이팅) — LLM 연동 전까지 보류(2026-08-02, 사용자 결정)**
    - **사용자가 정의한 3단계**: 보수적 = 기술적 교정 위주, 화자의 발화 자체는 바꾸지 않음(자막이나 보수적 교정을 요구한 문서). 일반 = 자연스러운 표현 제안·문장 구조 개선 허용. 리라이팅 = 문체 변경·문단 재구성까지 허용.
    - **왜 지금 못 만드나**: 이 엔진은 표준국어대사전·우리말샘·어문 규범을 조회해 **규범 위반 여부**를 판정한다. "이 표현이 자연스러운가", "이 문장 구조가 나은가", "이 문단을 어떻게 재구성할까"는 사전에 답이 없어 규범 조회로 도달할 수 없다. 일반·리라이팅은 기능 범위를 넓히는 일이 아니라 **없는 능력을 새로 붙이는 일**이다.
    - **필요한 것**: LLM(Claude API 등) 연동. 아키텍처 변경이라 API 키·비용과 함께, "확률적 추측으로 자동 수정하지 않는다"(`PRD.md` §5)는 최우선 원칙과 어떻게 공존시킬지 먼저 정해야 한다 — 최소한 제안 전용으로 못 박고 자동 교정 경로에는 절대 연결하지 않는 선.
    - **보수적 단계만은 지금도 가능하다**(참고용 설계, 착수 승인 안 받음): 어휘를 바꾸는 교정(비표준어·차별적 표현·옛 용어 치환)을 자동 적용 대신 확인 플래그로 강등하고, 표기 교정(띄어쓰기·맞춤법·외래어 표기·자막 부호)은 자동 유지. 기준선은 "표기를 고치는가 / 화자가 고른 낱말을 바꾸는가"다. 다만 세 단계를 함께 내놓지 못하면 반쪽이라, 사용자가 LLM 연동 전까지 강도 설정 자체를 만들지 않기로 했다.
    - **다음 세션 주의**: 사용자가 다시 꺼내기 전에는 이 기능을 제안하지 말 것. 꺼낸다면 LLM 연동 결정이 선행이다.

### 부분 완료 — 새로 만들지 말고 보탤 것

9. **테스트셋 확대** — **확인: `examples/eval/heldout.jsonl`에 정답 라벨 14건 + 실행기 `run_eval.py`가 이미 있다**(2026-07-25 생성). §8 체크리스트의 `[ ]`가 낡았던 것이라 `[~]`로 고쳤다. 남은 일은 수집·라벨링을 처음부터 하는 것이 아니라 **건수를 늘리는 것**이다.
10. **회고 "아쉬운 점" — 완료(2026-07-27)** — `docs/RETROSPECTIVE.md`에 "아쉬운 점" 절을 추가했다(온라인가나다 2단계 미구현, 테스트셋 14건의 한계, 배포 미완, 규범 개정 추적이 사람 손에 달린 점, 할 일이 흩어져 있었던 점). `docs/Review.md:40`이 요청한 내용은 이제 다 들어 있다.

### 문서 정리 (기능 아님)

11. **`docs/IMPLEMENTATION_LOG.md` 아카이브 2회차 — 완료(2026-07-27)** — §22~§30을 `docs/log-archive/2026-h2.md`로 옮겨 44,384자에서 약 7,100자로 줄였다(임계 이하). 아카이브 파일 제목 범위와 원본 상단 포인터를 §11~§30으로 갱신했다.
12. **`AGENTS.md`의 `§31`·`§32` 참조 — 완료(2026-07-27)** — 두 번호는 `PRD.md`가 아니라 `docs/IMPLEMENTATION_LOG.md`의 섹션이었다(§31 5차 실사용 감수, §32 리포트 축약 D + 사용목적 모드 E). `AGENTS.md`에 파일명을 명시했다.
13. **낡은 기록 정정 — 완료(2026-07-27)** — `docs/IMPLEMENTATION_LOG.md:100`(지역어 미연결)과 `:177`(D·E 미착수, `search_dialect` 중단)을 실제 코드 상태로 정정했고, §8 테스트셋 체크박스도 고쳤다. 남은 항목이 아니라 이 확인 작업의 결과 기록이다.

### 리팩터링

14. **`engine.py` 모듈 분할 (2,222줄 / 121,524자)** — 이 프로젝트에서 세션 토큰이 크게 나가는 첫 번째 원인은 이 파일 하나다(참고로 kiwipiepy는 로컬 C++ 분석기라 토큰을 전혀 쓰지 않는다 — 310MB는 메모리 비용이고 토큰과 무관하다. 2026-07-27 확인). 공개 함수는 `correct_*`/`check_*` 22개 + `correct_entries`(2010행)이고, 이미 기능별로 뭉쳐 있어 경계가 뚜렷하다. 제안하는 분할:
    - `kiwi_adapter.py` — `_kiwi` 싱글턴, `register_custom_words`, `_straddling_tokens`/`_token_containing`/`_tokenization_unstable_near` 등 토큰 조회 도구
    - `spacing.py` — `_mechanical_respace`, `correct_particle_spacing`, `correct_compound_spacing`, `correct_aux_verb_spacing`, `_protect_unfounded_joining`
    - `replacements.py` — `correct_always_wrong`, `correct_nonstandard_terms`, `correct_discriminatory_terms`, `correct_former_terms`, `_apply_replacements`, 조사 이형태 처리
    - `spelling.py` — `check_spelling`과 그 판정 도구(`_tensified_headword_variant`, `_is_reduplication`, `_is_native_compound`, `_unknown_content_words`)
    - `loanwords.py` / `subtitle.py` / `dialect.py` — 외래어, 자막 전용 부호 규칙, 사투리 모드
    - `engine.py` — `correct_entries` 오케스트레이션만 남긴다
    - **반드시 지킬 것 ①**: 순수 이동만 한다. 로직을 함께 손보면 150건 테스트가 회귀를 잡아주는 효과가 사라진다. 이동 후 전체 테스트가 그대로 통과해야 하고, 통과하지 않으면 이동이 잘못된 것이다.
    - **반드시 지킬 것 ②**: `_kiwi`는 프로세스에 **하나만** 있어야 한다. 모듈마다 `Kiwi()`를 만들면 메모리가 배로 늘고(§10 배포 한도와 직결), `register_custom_words()`로 등록한 고유명사가 다른 모듈에 전달되지 않아 "kiwi가 이름을 쪼개는" 버그가 되살아난다. 반드시 `kiwi_adapter.py` 하나에서 만들고 나머지는 import만 한다.
    - **기대 효과의 한계**: 엔진 전체를 봐야 하는 작업에서는 토큰이 줄지 않는다. 한 영역만 건드리는 작업(예: 띄어쓰기 규칙 하나 추가)에서 파일 하나만 읽으면 되는 것이 이득이다.

### 이 목록에 넣지 않은 것 (v2 로드맵·정책, §9/§10)

로그인/계정, 결제, 도메인별 전문용어 사전, Supabase 저장 기간·개인정보 처리 정책, 배포 방식·성능 목표 등. 착수 대기 작업이 아니라 방향 결정 사항이므로 §9·§10에 그대로 둔다.
