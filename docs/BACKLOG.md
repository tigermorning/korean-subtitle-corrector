# 밀린 작업 목록

`PRD.md`에서 분리된 문서. **여기는 아직 끝나지 않은 작업만 남긴다** — 끝난
항목은 지우거나 한 줄로 줄이고(경위는 `docs/IMPLEMENTATION_LOG.md`·
`docs/log-archive/`에 이미 있음), 새로 미룬 일이 생기면 여기 추가한다. 번호는
코드 주석·테스트가 `docs/BACKLOG.md` N번으로 직접 인용하므로, 항목이 끝나도
번호 자체는 지우지 않고 한 줄 요약으로 남긴다.

**세션을 시작하면 다른 작업보다 먼저 이 파일을 읽고 사용자에게 보고할 것.**

---

## 열려 있는 항목

**다음 세션 시작 순서(2026-09-02 갱신, 작업자자료 4번 착수로 갱신)**:
31번은 중국어 세칙 제2항까지 구현했고 남은 부분(영·독·프)은 발음사전 도입
여부를 사용자와 먼저 정해야 한다 — **사용자가 먼저 꺼내기 전에는 제안하지
말 것.** 작업자자료 4번(이미지 62장 규범 표)이 남은 유일한 항목 — 접근은
이미 확인됐고 10/62 완료(§83), 52장 남음(아래 4번 항목의 주제 목록 참고).
1·7·17번은 사용자 결정 전까지 착수하지 말 것, 5번은 외부 서버 복구 대기.

### 1 · 7. 온라인가나다 판례 활용 — 착수 불가 고정

**결정(2026-08-02)**: 온라인가나다 판례를 미리 훑어 축적하는 작업(2단계 연동
포함)을 앞으로 하지 않는다. 그 결과 **1번(유사 판례 검색 실측 감수)**은
`PRECEDENTS`를 채우는 선행 작업 자체가 없어져 착수 불가로 고정됐고, **7번
(온라인가나다 연동 2단계, 코드 0건)**도 같은 결정에 묶인다. 이미 쌓인
`docs/GRAMMAR_PRECEDENTS_TABLE.md`·`subtitle_corrector/gananda_precedents.py`는
지우지 않고, 실제 애매 사례를 만났을 때 그 자리에서 확인해 쓰는 것까지 막는
결정은 아니다. **사용자가 먼저 꺼내기 전에는 판례 스터디·2단계 연동을 제안하지
말 것.**

### 17. 교정 강도 선택(보수적/일반/리라이팅) — LLM 연동 선행

사용자가 정의한 3단계: 보수적(기술적 교정 위주, 발화 자체는 유지) / 일반(자연
스러운 표현 제안 허용) / 리라이팅(문체·문단 재구성 허용). 이 엔진은 사전·규범
조회로 **규범 위반 여부**만 판정하므로, 일반·리라이팅은 없는 능력(자연스러움
판단)을 새로 붙이는 일이다 — LLM 연동이 선행돼야 하고, "확률적 추측으로 자동
수정하지 않는다"(`PRD.md` §5)는 원칙과 어떻게 공존시킬지(최소한 제안 전용으로
못 박는 선) 먼저 정해야 한다. **사용자가 다시 꺼내기 전에는 제안하지 말 것.**

### 5. 지역어 종합 정보 API 500 장애 — 외부 요인, 대기

**2026-09-01 재확인: 여전히 500이다.** 실제 클라이언트가 쓰는 파라미터
(`apiKey`·`searchWord`)로 보내면 HTTP 500 + HTML이 그대로 온다. 같은 날 어문 규범
API의 403이 User-Agent 차단으로 밝혀져(§79) 이쪽도 같은 원인인지 확인했으나 아니었다 —
헤더를 붙여도 500이다. 서버 복구를 계속 기다린다.

원 요청(`정구지`·`머스마`·`무싯날`·`감자` 4개 질의)으로 재확인, 전부 HTTP 500 +
HTML 오류 페이지. `search_dialect()`가 실패를 삼키고 `[]`를 돌려주므로 "장애"와
"매칭 없음"이 구분 안 된다 — **상태 확인은 반드시 원 요청으로 할 것.** 서버가
복구되기 전에는 우리 쪽에 할 일이 없다. 사투리 assist의 표준어→사투리 역방향이
막혀 있는 근본 원인(§12).

### 31. 외래어 표기법 언어별 세칙 전면 도입 — 착수 대기 (2026-08-14 사용자 지시)

국립국어원 「외래어 표기법」은 기본 원칙 5개 조항 아래 **언어별 표기 세칙**이
방대하다. 지금은 용례 조회(표준국어대사전·우리말샘·kornorms)에만 기대고 있어
세칙 자체를 규칙으로 갖고 있지 않다.

**우선순위(사용자 지정): 영어 · 독일어 · 프랑스어 · 중국어 · 일본어.** 장기적
으로는 전 언어 반영. 착수 지점(작업자 자료에서 이미 옮겨 적음):

- 기본 원칙 제3항(받침은 `ㄱㄴㄹㅁㅂㅅㅇ`만)·제4항(파열음에 된소리 금지)은
  기계가 잰다. **단, 제4항엔 예외가 있다** — 태국어 세칙은 `t→ㄸ`, `k→ㄲ`,
  `p→ㅃ`. 언어를 모른 채 된소리 검사를 걸면 태국어 표기를 전부 오탐지로 잡는다
  — 세칙을 언어별로 갖춰야 하는 이유.
- 영어: 무성 파열음 [p t k]는 단모음 뒤에서만 받침, 그 외엔 `으` / 유성
  파열음 [b d g]는 언제나 `으` / **[ʃ]에 `쉬`를 쓰지 않는다** / 약어 `R`=아르,
  `&`=앤드.
  일본어: 어두/어중·어말에 따라 청음·유성 갈림(카키쿠케코 ↔ 가기구게고),
  `zu`·`su`→`즈`·`스`. 중국어: 한어 병음 대조표, `z→ㅉ`, `c→ㅊ`, `s→ㅆ`.
- 언어마다 무엇을 대조하는지가 다르다(영·독·프는 발음기호, 스페인·이탈리아·
  러시아는 철자, 중국어는 한어 병음). 예외 규정(관용이 이기는 자리, 등록상표,
  뜻이 갈리는 복수 표기 등)은 `docs/LOANWORD_EXCEPTIONS.md`에 모아 뒀다.

**"먼저 할 일" 해결됨(2026-09-01).** `exampleReqList.do`는 확인대로 용례만
준다. 세칙 원문은 `regltnView.do?regltn_code=0003` 열람 페이지에 실제로 다
있다 — 단 이 페이지는 자바스크립트로 본문을 그려서 **정적 fetch(WebFetch)로는
제3장(표기 세칙) 이후가 잘려 나온다.** Browser 도구(JS 렌더링)로 열어야
전문이 보인다는 것까지 실측으로 확인했다. 우선순위 5개 언어(영어·독일어·
프랑스어·중국어·일본어) 세칙 조문을 전부 이 방법으로 가져와
`docs/LOANWORD_TRANSCRIPTION_RULES.md`에 옮겨 뒀다.

**다섯 언어의 난이도가 균일하지 않다는 것도 확인했다.** 일본어·중국어 세칙은
가나·한어병음이라는 **문자 체계** 기준이라 표 4·표 5 대조표만 있으면 되는데,
**영어·독일어·프랑스어 세칙은 전부 국제음성기호(IPA) 전사 기준**(`cat[kæt]`
처럼)이라 엔진이 보는 한글 표기만으로는 판정 근거(원어·발음기호)가 없다 —
(1)한글→원어 역산 (2)원어→발음기호 변환(영어는 CMU 발음사전류, 독일어·
프랑스어는 별도 소스 필요, 지금 저장소에 없음) (3)발음기호→세칙 적용의
인프라가 선행돼야 한다. **어느 발음사전을 쓸지부터 사용자와 정해야 착수할 수
있다 — 아직 안 함.**

**중국어 세칙 제2항(ㅈ·ㅉ·ㅊ 뒤 ㅑㅖㅛㅠ 금지, 쟈→자 등)은 구현 완료
(2026-09-01, §82).** 유일하게 원어 정보 없이 한글 표기만으로 기계적 판정이
가능했던 조항이다. `check_palatal_glide_loanword()`(`engine/loanwords.py`),
확인 플래그 전용, kornorms 미등재 신조어만 대상(등재된 건 `correct_loanwords()`가
먼저 처리). kornorms 실측으로 이 12음절이 중국어 전용이 아니라 영어·독일어·
프랑스어·에스파냐어·일본어에도 걸쳐 있는 일반 표기 관행임을 확인했다 — 상세는
§82. **일본어 촉음·장모음, 중국어 성조 규칙은 영·독·프와 같은 벽**(원어 가나/
병음 정보가 없어 한글 표기만으로 위반 여부를 판정할 수 없음)이라 미착수.

상세 조문·기술 격차 설명은 `docs/LOANWORD_TRANSCRIPTION_RULES.md` 참고.

### 작업자 실무 자료 반영 — 진행 중 (2026-08-11 수령)

사용자가 실제 자막 작업을 하며 만든 자료. **`../subtitle-tc-generator/rules/sources/작업자-자료/`**
(자막 편집기 저장소)에 원본(.docx/.xlsx)·텍스트 추출본·이미지 127장 판정
(`이미지-판정.md`)이 있다. 자막 문법이 아니라 한국어 규범이므로 편집기가 아니라
이 저장소 몫이고, 편집기는 `--korean` 레인으로 결과만 받는다.

**할 일 넷**(우선순위: 문장부호 NOTA-014·016~021, 단위 NOTA-051~053은 자막에도
바로 걸리므로 먼저):
1. 번역투 — 이중 피동, 사역(`-게 하다`), 사물 주어, 최상급 오용, `N의 N`,
   `-수 있다` 남용, 직접→간접 인용
2. 차별적 언어·바뀐 용어(간질→뇌전증 등)
3. **사용자 체크리스트 — 채점 정상화 완료(2026-09-01, §81), 새 규칙 구현은
   그대로 남음.** 30건(`examples/eval/작업자자료.jsonl`, `run_eval.py
   작업자자료.jsonl`로 실행). `expect_flag` 채점이 `judge()`의 텍스트 일치
   검사에 먼저 걸려 사실상 죽어 있던 것을 확인 — heldout.jsonl처럼
   `gold=input`(무변경) + `basis`에 `suggested_fix` 명시 관례로 맞춰 w07·w12·
   w20 3건 전환(2/30 -> 5/30 = 16.7%). w28(포르셰/포르쉐)은 입력/정답이
   거꾸로 적힌 라벨 오류였다 — 정정. w23·w27은 kornorms·사전 어디에도 없어
   조회불가로 표시(w23은 31번 선행 필요). w29(커리/카레)는 kornorms가 '커리'를
   인명 'Cori'의 오표기로만 등재해 둬서(33번과 같은 동형이의어) 텍스트만으로
   못 고치는 받아들인 한계로 남김. **나머지 21건은 전부 엔진이 아직 구현하지
   않은 개별 규칙**(명사+명사+하다 재분리, 부사·보조용언·단위 띄어쓰기,
   완전/완전히, 아니오/아니요, -구먼/-구만, 외래어 받침, 형용사형 국명, 인명·
   상표 관용 표기)이다 — 라벨은 대부분 검증됐으니(`verify_eval_labels.py`)
   새로 만들려면 항목별로 `grammar-rule-verify-then-code`를 거칠 것.
4. **이미지 62장의 규범 표 — 21장 검토(2026-09-02, §85·§86), 새 규칙 2건(자동
   교정 1·확인 플래그 1), 41장 남음.** 문장부호 10장은 §83이 이미 끝냈다.
   원본 `.docx`에서 문단 순서대로 이미지를 추출하는 방법은
   `이미지-판정.md`의 "방법 기록" 그대로(같은 순서 = NOTA 번호) — 세션 스크래치패드
   추출본은 매번 사라지므로, 다시 뽑으려면 `docs/log-archive/2026-h2.md`나 §85가
   아니라 이 방법을 그대로 다시 돌릴 것.

   **NOTA-013~021(문장부호) — 재확인 결과 전부 처리됨(§83이 맞았다, 오해 정정).**
   013(범위 표기)·015(쉼표)·017(물결표/붙임표)은 옆 저장소(`subtitle-tc-generator`)
   가 이미 함, 014(날짜 온점·로캘별 소수점 표기)는 번역 판단이라 스코프 밖,
   016(말줄임표)·018(따옴표)·021(전각반각)은 `options.py`가 커버, 019(쌍점)·
   020(`&`)·NOTA-053(단위 대소문자)은 §83이 구현. **문장부호 그룹은 완결.**

   **NOTA-001~012 — 재검토, 5개 구현.**
   - 001~003(띄어쓰기 판별 단계, 품사 기준) — 개념 설명 자체가 이 엔진의
     kiwi POS 기반 판정 원리와 같다. 새 코드 없음.
   - **004(의존명사 문장 첫머리 금지) — 구현.** `나름`·`때문`이 앞말 없이
     줄 첫머리에 오면 확인 플래그(`dependent_nouns.check_dependent_noun_sentence_start`).
     자동 교정은 안 함 — 자막은 문장이 여러 줄로 쪼개져 앞 줄의 꾸밈말을
     이어받는 경우가 흔해서다. 실제 자막 4개 파일 1,926줄로 오탐률 확인:
     1건뿐이고 그나마 다른 낱말('척')의 오태깅이었다.
   - 005(변경된 용어: 터키→튀르키예, 나트륨→소듐 등) — **자동화 금지 대상으로
     확정.** 이미지 자체가 '논란'·'회귀'(되돌아간 표기도 있음) 라벨을 달아 둠 —
     정치·과학용어 표기 다툼은 사람 판단.
   - 006(순화어 주의) — **자동화 금지 대상으로 확정.** 이미지 제목이 "주의"이고
     실제로 안 쓰이는 억지 순화어(곤돌라→방승기 등)를 스스로 예로 듦.
   - **007(제-차 표기) — 구현.** 접두사 '제'를 뒤 말에 붙인다
     (`affix.correct_ordinal_prefix_je`). kiwi가 순번의 '제'(XPN)와 대명사
     '제'/저의(NP)를 정확히 갈라 태깅해 자동 교정. '차'는 붙여도 띄워도
     맞으므로 안 건드림.
   - 008~009(숫자 한글/아라비아 표기 원칙, 사전 신뢰도 안내) — "나음"(권장)
     이지 오류가 아니고, 사전 종류 안내는 규칙이 아니라 참고 정보. 새 코드 없음.
   - 010(합성어 예시 목록) — 이미 `word_exists` 그대로. 새 코드 없음.
   - 011~012(구어체 나이 표기·번역투 'N의 명사') — **자막 문법**(구어체 대사 vs
     문어체 화면자막 스타일)이라 이 저장소가 아니라 옆 편집기 몫. 012는 설사
     이 저장소 몫이었어도 등록 여부 판단이 아니라 자연스러움 판단이라
     원리 2(모호할 때 자동 교정 금지) 대상.

   **번호-내용 매핑 주의(2026-09-02 발견)**: `.docx`의 캡션 텍스트박스가 인접
   이미지로 밀려 붙는 문서 구조 때문에, 추출 스크립트가 매긴 NOTA-N 번호가
   `이미지-판정.md`가 말하는 실제 내용과 어긋나는 경우가 있다(예: "종결어미
   -요/이요"라고 캡션이 달린 자리의 실제 이미지는 '부딪치다/부딪히다'였다).
   아래 "맞춤법 그룹" 항목은 그래서 정확한 NOTA-N 대신 **내용으로** 적는다 —
   번호는 재확인 전까지 참고만 할 것.

   **맞춤법 그룹(054~062 부근, 9장) — 재검토, 새 자동화 대상 0건.**
   - 명사구 붙임/띄움 목록(오늘 밤·지난주·이틀간 등) — 이미 `word_exists` 기반
     합성어 판정 그대로. 새 코드 없음.
   - 부딪치다/부딪히다(능동/피동, 문맥에 따라 갈림), 종결어미 -대/-데(전달/경험,
     뜻이 다름), 한번/한 번(강조·시도 vs 수효, 뜻이 다름), 잘못되다/잘 못되다/
     잘 안되다(뜻이 다름) — **전부 원리 2(모호할 때 자동 교정 금지) 대상.**
     맞춤법 오류가 아니라 뜻이 다른 낱말·표현을 고르는 문제라 자동 판단 불가.
   - **-ㄹ걸(어미)/걸(의존명사, =것을) — 검토했으나 자동화 불가로 확정.**
     "모를걸?"(안 띄움)은 kiwi가 어미(EF) 한 토큰으로, "모를 걸?"(띄움)은
     의존명사+조사(NNB+JKO) 두 토큰으로 읽는다 — 서로 다른 태그처럼 보이지만
     **원문의 띄어쓰기를 그대로 따라가는 순환 신호**임을 실측으로 확인했다
     (같은 문장을 띄우거나 붙여 넣으면 kiwi 판독이 그때그때 따라 바뀐다).
     이 신호로 사용자의 원래 의도를 가를 수 없다.
   - 뿐(조사/의존명사) — 이미 `correct_bun_spacing`이 처리. -ㄹ뿐더러(어미)는
     드문 자리라 보류.
   - 맨(관형사)/맨-(접사) — 이미지가 틀린 예시 없이 올바른 용례만 보여 준다.
     고칠 오류가 없어 보류.
   - 보조 용언 띄어쓰기 분류표 — 기존 `_aux_verb_spacing`(제47항) 체계와
     같은 내용의 참고표. 새 코드 없음.

   남은 것: 단위성 명사·숫자 나머지, 외래어 세칙(022~041 — 31번과 겹침, 이미
   처리한 5개 언어 재확인 필요), 고유명사 띄어쓰기(042~050 — 브랜드명·인명
   표기 다툼이 많아 005·006과 같은 위험이 예상됨, 검토 시 주의), 분야별
   용어·말습관(063~064) — 주제별 목록은 `이미지-판정.md` 참고. **005·006·011·012는
   위 이유로 착수 금지 — 사용자가 먼저 꺼내기 전에는 제안하지 말 것.**

> **주의**: 자료 절반 이상은 관용 표현·말장난·욕설 수위·인물 관계처럼 **사람이
> 판단할 것**이다. 도구가 대신하려 들면 사고가 난다. 반영 판정은
> `../subtitle-tc-generator/rules/sources/작업자-자료/반영-계획.md` 참고.

---

## 보류 결정 (사용자가 다시 꺼내기 전엔 제안 금지)

- **욕설 마스킹 자동화** — 안 함(2026-08-02). 가리는 범위가 이용 연령 등급마다
  달라 텍스트만으로 정할 근거가 없다.
- **클라우드 배포** — 안 함(종결). 근거·경위는 `PRD.md` §11.

## 받아들인 한계

- **26. '명사+명사+하다'에서 놓치는 정당한 붙임** — 앞 명사가 관형어인지
  목적어인지 표면으로 못 가르므로, "붙여선 안 될 것을 붙이는 것"보다 "붙여야
  할 것을 못 붙이는 것"을 택했다(`생일 선물 받았어`가 그대로 남음). 재개 조건:
  앞뒤 명사가 사전에 한 명사구로 등재됐는지 대조하는 방법이 생기면.

---

## 완료된 항목 (번호는 코드/테스트가 인용 — 상세는 로그 아카이브)

- **2.** 명사+드리다 오탐지 — 해결(2026-08-05, `docs/IMPLEMENTATION_LOG.md` §67).
- **32.** 좌우 병합 애매 판정이 플래그조차 안 내던 것 — 해결(2026-09-01, §80).
  제시된 두 방향 중 **1번(애매해도 플래그로 사람에게 확인)** 을 택했다.
- **3.** 제42항 의존명사 6개(들/뿐/만/지/차/판) — 해결(2026-08-05, §69).
- **4.** 제44항 수 표기(몇만 등) — 승격 불가로 종결(2026-08-04, §64, 사전
  미등재라 근거 없음).
- **6.** 표준국어대사전 사전 내용 API(`view.do`) 연동 — 해결(2026-08-05, §72).
- **9.** 테스트셋 확대(14→41건) — 완료(2026-08-03, `docs/log-archive/2026-h2.md` §51).
- **14.** `engine.py` 모듈 분할(3,172줄→16개 모듈) — 완료(2026-08-02, log-archive §46).
- **15.** 조사 '에서'를 '에'+'서다'로 가르던 과교정 — 해결(2026-08-02, log-archive §45).
- **16.** 거리 이름 `대로/로/가` 띄어쓰기 플래그 — 해결(2026-08-05, §65).
- **18.** 사전 미등재 고유명사 분리 오탐지(`한짓골`) — 해결(2026-08-02, log-archive §44).
- **19.** 감탄사 표제어 분할로 인한 오탐지(`에라이!`) — 해결(2026-08-02, log-archive §44).
- **23.** 조사+보조용언 분리 제안 들쭉날쭉 — 해결(2026-08-05, §70).
- **24.** 평가셋 실패 4건(`안절부절` 등) — 전부 해결(2026-08-03~04, §63/log-archive §51).
- **25.** 사투리 표지 사전 다시 채우기 — 해결(2026-08-05, §71, 감지 34개/변환 18개).
- **27.** `정도 됩니다`→`정도됩니다` 과교정 — 해결(2026-08-04, §58).
- **28.** 인명·지명 표기 플래그 문구 개선 — 해결(2026-08-04, §61).
- **29.** 붙임/분리/접사 규칙이 서로 되돌리던 것 — 해결(2026-08-04, §60, 순서
  고정이 유일하게 안전).
- **30.** kiwi가 '하'를 VV로 태깅하면 분리 규칙이 안 걸리던 것 — 플래그로
  해결(2026-08-04, §60 인접).
- **33.** 방언 동형이의어가 `word_exists()`를 표준으로 오판(빠리·커리) —
  `is_standard_word()` 분리로 해결(2026-09-01, §77). 방언은 실존 낱말로
  인정해야 하는 다른 call site(affix·spacing)는 미감사.

---

## 이 목록에 넣지 않은 것 (v2 로드맵·정책, `PRD.md` §9/§10)

로그인/계정, 결제, 도메인별 전문용어 사전, Supabase 저장 기간·개인정보 처리
정책, 배포 방식·성능 목표 등. 착수 대기 작업이 아니라 방향 결정 사항이므로
PRD에 그대로 둔다.
